---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Grundlegendes zu Aktionsfiltern | Microsoft Docs
author: rick-anderson
description: Das Ziel dieses Tutorials ist es, Aktionsfilter zu erklären. Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controlleraktion anwenden können -- oder einen gesamten Controller...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: 75ba7b1dce280a45cd092de97c464eade5f49838
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542182"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="bd1b4-104">Grundlegendes zu Aktionsfiltern (C#)</span><span class="sxs-lookup"><span data-stu-id="bd1b4-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="bd1b4-105">von [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="bd1b4-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="bd1b4-106">PDF herunterladen</span><span class="sxs-lookup"><span data-stu-id="bd1b4-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="bd1b4-107">Das Ziel dieses Tutorials ist es, Aktionsfilter zu erklären.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="bd1b4-108">Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controlleraktion -- oder einen gesamten Controller - anwenden können, die die Art und Weise ändert, in der die Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="bd1b4-109">Grundlegendes zu Aktionsfiltern</span><span class="sxs-lookup"><span data-stu-id="bd1b4-109">Understanding Action Filters</span></span>

<span data-ttu-id="bd1b4-110">Das Ziel dieses Tutorials ist es, Aktionsfilter zu erklären.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="bd1b4-111">Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controlleraktion -- oder einen gesamten Controller - anwenden können, die die Art und Weise ändert, in der die Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="bd1b4-112">Das ASP.NET MVC-Framework enthält mehrere Aktionsfilter:</span><span class="sxs-lookup"><span data-stu-id="bd1b4-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="bd1b4-113">OutputCache – Dieser Aktionsfilter speichert die Ausgabe einer Controlleraktion für einen bestimmten Zeitraum zwischen.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="bd1b4-114">HandleError – Dieser Aktionsfilter behandelt Fehler, die beim Ausführen einer Controlleraktion ausgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="bd1b4-115">Autorisieren – Mit diesem Aktionsfilter können Sie den Zugriff auf einen bestimmten Benutzer oder eine bestimmte Rolle beschränken.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="bd1b4-116">Sie können auch eigene benutzerdefinierte Aktionsfilter erstellen.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="bd1b4-117">Sie können z. B. einen benutzerdefinierten Aktionsfilter erstellen, um ein benutzerdefiniertes Authentifizierungssystem zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="bd1b4-118">Sie können auch einen Aktionsfilter erstellen, der die von einer Controlleraktion zurückgegebenen Ansichtsdaten ändert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="bd1b4-119">In diesem Tutorial erfahren Sie, wie Sie einen Aktionsfilter von Grund auf erstellen.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="bd1b4-120">Wir erstellen einen Protokollaktionsfilter, der verschiedene Phasen der Verarbeitung einer Aktion im Visual Studio-Ausgabefenster protokolliert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="bd1b4-121">Verwenden eines Aktionsfilters</span><span class="sxs-lookup"><span data-stu-id="bd1b4-121">Using an Action Filter</span></span>

<span data-ttu-id="bd1b4-122">Ein Aktionsfilter ist ein Attribut.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-122">An action filter is an attribute.</span></span> <span data-ttu-id="bd1b4-123">Sie können die meisten Aktionsfilter entweder auf eine einzelne Controlleraktion oder auf einen gesamten Controller anwenden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="bd1b4-124">Beispielsweise macht der Datencontroller in Liste 1 `Index()` eine Aktion mit dem Namen verfügbar, die die aktuelle Uhrzeit zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="bd1b4-125">Diese Aktion ist `OutputCache` mit dem Aktionsfilter dekoriert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="bd1b4-126">Dieser Filter bewirkt, dass der von der Aktion zurückgegebene Wert für 10 Sekunden zwischengespeichert wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="bd1b4-127">**Auflistung 1 –`Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="bd1b4-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="bd1b4-128">Wenn Sie die `Index()` Aktion wiederholt aufrufen, indem Sie die URL /Daten/Index in die Adressleiste Ihres Browsers eingeben und mehrmals auf die Schaltfläche Aktualisieren klicken, wird die gleiche Zeit für 10 Sekunden angezeigt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="bd1b4-129">Die Ausgabe `Index()` der Aktion wird für 10 Sekunden zwischengespeichert (siehe Abbildung 1).</span><span class="sxs-lookup"><span data-stu-id="bd1b4-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="bd1b4-130">[![Zwischengespeicherte Zeit](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="bd1b4-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="bd1b4-131">**Abbildung 01**: Zwischenspeichern der Zeit ([Klicken Sie hier, um das Bild in voller Größe anzuzeigen](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="bd1b4-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="bd1b4-132">In Listing 1 wird ein `OutputCache` einzelner Aktionsfilter – `Index()` der Aktionsfilter – auf die Methode angewendet.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="bd1b4-133">Bei Bedarf können Sie mehrere Aktionsfilter auf dieselbe Aktion anwenden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="bd1b4-134">Sie können z. B. `OutputCache` sowohl `HandleError` den Aktionsfilter als auch den Aktionsfilter auf dieselbe Aktion anwenden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="bd1b4-135">In Liste 1 `OutputCache` wird der Aktionsfilter auf die `Index()` Aktion angewendet.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="bd1b4-136">Sie können dieses Attribut `DataController` auch auf die Klasse selbst anwenden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="bd1b4-137">In diesem Fall wird das Ergebnis, das von einer vom Controller ausgesetzten Aktion zurückgegeben wird, für 10 Sekunden zwischengespeichert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="bd1b4-138">Die verschiedenen Filtertypen</span><span class="sxs-lookup"><span data-stu-id="bd1b4-138">The Different Types of Filters</span></span>

<span data-ttu-id="bd1b4-139">Das ASP.NET MVC-Framework unterstützt vier verschiedene Filtertypen:</span><span class="sxs-lookup"><span data-stu-id="bd1b4-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="bd1b4-140">Autorisierungsfilter – `IAuthorizationFilter` Implementiert das Attribut.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="bd1b4-141">Aktionsfilter – Implementiert `IActionFilter` das Attribut.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="bd1b4-142">Ergebnisfilter – Implementiert `IResultFilter` das Attribut.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="bd1b4-143">Ausnahmefilter – Implementiert `IExceptionFilter` das Attribut.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="bd1b4-144">Filter werden in der oben aufgeführten Reihenfolge ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="bd1b4-145">Autorisierungsfilter werden beispielsweise immer ausgeführt, bevor Aktionsfilter und Ausnahmefilter immer nach jedem anderen Filtertyp ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="bd1b4-146">Autorisierungsfilter werden verwendet, um Authentifizierung und Autorisierung für Controlleraktionen zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="bd1b4-147">Der Filter Autorisieren ist beispielsweise ein Beispiel für einen Autorisierungsfilter.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="bd1b4-148">Aktionsfilter enthalten Logik, die vor und nach der Ausführung einer Controlleraktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="bd1b4-149">Sie können z. B. einen Aktionsfilter verwenden, um die Ansichtsdaten zu ändern, die von einer Controlleraktion zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="bd1b4-150">Ergebnisfilter enthalten Logik, die vor und nach der Ausführung eines Ansichtsergebnisses ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="bd1b4-151">Sie können z. B. ein Ansichtsergebnis direkt ändern, bevor die Ansicht im Browser gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="bd1b4-152">Ausnahmefilter sind der letzte auszuführende Filtertyp.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="bd1b4-153">Sie können einen Ausnahmefilter verwenden, um Fehler zu behandeln, die entweder durch Die Controlleraktionen oder controller-Aktionsergebnisse ausgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="bd1b4-154">Sie können auch Ausnahmefilter verwenden, um Fehler zu protokollieren.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="bd1b4-155">Jeder unterschiedliche Filtertyp wird in einer bestimmten Reihenfolge ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="bd1b4-156">Wenn Sie die Reihenfolge steuern möchten, in der Filter desselben Typs ausgeführt werden, können Sie die Order-Eigenschaft eines Filters festlegen.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="bd1b4-157">Die Basisklasse für alle `System.Web.Mvc.FilterAttribute` Aktionsfilter ist die Klasse.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="bd1b4-158">Wenn Sie einen bestimmten Filtertyp implementieren möchten, müssen Sie eine Klasse erstellen, die von der Basisfilterklasse `IActionFilter` `IResultFilter`erbt `IExceptionFilter` und eine oder mehrere der `IAuthorizationFilter`, , oder Schnittstellen implementiert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="bd1b4-159">Die Basis-ActionFilterAttribute-Klasse</span><span class="sxs-lookup"><span data-stu-id="bd1b4-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="bd1b4-160">Um Ihnen das Implementieren eines benutzerdefinierten Aktionsfilters zu erleichtern, enthält `ActionFilterAttribute` das ASP.NET MVC-Framework eine Basisklasse.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="bd1b4-161">Diese Klasse implementiert `IActionFilter` sowohl `IResultFilter` die als auch Schnittstellen `Filter` und erbt von der Klasse.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="bd1b4-162">Die Terminologie hier ist nicht ganz konsistent.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="bd1b4-163">Technisch gesehen ist eine Klasse, die von der ActionFilterAttribute-Klasse erbt, sowohl ein Aktionsfilter als auch ein Ergebnisfilter.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="bd1b4-164">Im lockeren Sinne wird jedoch das Wort Aktionsfilter verwendet, um auf jede Art von Filter im ASP.NET MVC-Framework setzt sich.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="bd1b4-165">Die `ActionFilterAttribute` Basisklasse verfügt über die folgenden Methoden, die Sie überschreiben können:</span><span class="sxs-lookup"><span data-stu-id="bd1b4-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="bd1b4-166">OnActionExecuting – Diese Methode wird aufgerufen, bevor eine Controlleraktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="bd1b4-167">OnActionExecuted – Diese Methode wird aufgerufen, nachdem eine Controlleraktion ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="bd1b4-168">OnResultExecuting – Diese Methode wird aufgerufen, bevor ein Controlleraktionsergebnis ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="bd1b4-169">OnResultExecuted – Diese Methode wird aufgerufen, nachdem ein Controlleraktionsergebnis ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="bd1b4-170">Im nächsten Abschnitt erfahren Sie, wie Sie jede dieser verschiedenen Methoden implementieren können.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="bd1b4-171">Erstellen eines Protokollaktionsfilters</span><span class="sxs-lookup"><span data-stu-id="bd1b4-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="bd1b4-172">Um zu veranschaulichen, wie Sie einen benutzerdefinierten Aktionsfilter erstellen können, erstellen wir einen benutzerdefinierten Aktionsfilter, der die Phasen der Verarbeitung einer Controlleraktion im Visual Studio-Ausgabefenster protokolliert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="bd1b4-173">Unsere `LogActionFilter` ist in Listing 2 enthalten.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="bd1b4-174">**Auflistung 2 –`ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="bd1b4-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="bd1b4-175">In Listing 2 `OnActionExecuting()` `OnActionExecuted()`rufen `OnResultExecuting()`die `OnResultExecuted()` , , `Log()` und die Methoden die Methode auf.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="bd1b4-176">Der Name der Methode und die aktuellen `Log()` Routendaten werden an die Methode übergeben.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="bd1b4-177">Die `Log()` Methode schreibt eine Nachricht in das Visual Studio-Ausgabefenster (siehe Abbildung 2).</span><span class="sxs-lookup"><span data-stu-id="bd1b4-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="bd1b4-178">[![Schreiben in das Visual Studio-Ausgabefenster](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="bd1b4-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="bd1b4-179">**Abbildung 02**: Schreiben in das Visual Studio-Ausgabefenster ([Klicken Sie hier, um das Bild in voller Größe anzuzeigen](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="bd1b4-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="bd1b4-180">Der Home-Controller in Liste 3 veranschaulicht, wie Sie den Aktionsfilter Protokoll auf eine gesamte Controllerklasse anwenden können.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="bd1b4-181">Immer wenn eine der vom Home-Controller verfügbar `Index()` gemachten `About()` Aktionen – entweder die Methode oder die Methode – aufgerufen wird, werden die Phasen der Verarbeitung der Aktion im Visual Studio-Ausgabefenster protokolliert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="bd1b4-182">**Auflistung 3 –`Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="bd1b4-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="bd1b4-183">Zusammenfassung</span><span class="sxs-lookup"><span data-stu-id="bd1b4-183">Summary</span></span>

<span data-ttu-id="bd1b4-184">In diesem Tutorial wurden Sie mit ASP.NET MVC-Aktionsfiltern vorgestellt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="bd1b4-185">Sie haben die vier verschiedenen Filtertypen gelernt: Autorisierungsfilter, Aktionsfilter, Ergebnisfilter und Ausnahmefilter.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="bd1b4-186">Sie haben auch `ActionFilterAttribute` etwas über die Basisklasse gelernt.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="bd1b4-187">Schließlich haben Sie gelernt, wie Sie einen einfachen Aktionsfilter implementieren.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="bd1b4-188">Wir haben einen Protokollaktionsfilter erstellt, der die Phasen der Verarbeitung einer Controlleraktion im Visual Studio-Ausgabefenster protokolliert.</span><span class="sxs-lookup"><span data-stu-id="bd1b4-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="bd1b4-189">[Zurück](asp-net-mvc-routing-overview-cs.md)
> [Weiter](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="bd1b4-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
