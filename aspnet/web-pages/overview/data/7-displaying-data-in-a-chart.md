---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Anzeigen von Daten in einem Diagramm mit ASP.NET Webseiten (Razor) | Microsoft Docs
author: rick-anderson
description: In diesem Kapitel wird erläutert, wie Daten in einem Diagramm angezeigt werden. In den vorherigen Kapiteln haben Sie gelernt, wie Daten manuell und in einem Raster angezeigt werden. In diesem Kapitel werden...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542884"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="bd241-105">Anzeigen von Daten in einem Diagramm mit ASP.NET Webseiten (Razor)</span><span class="sxs-lookup"><span data-stu-id="bd241-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="bd241-106">von [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="bd241-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="bd241-107">In diesem Artikel wird erläutert, wie Sie mithilfe des Hilfsplans ein `Chart` Diagramm zum Anzeigen von Daten auf einer ASP.NET Webseiten-Website (Razor) verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="bd241-108">**Was Sie lernen:**</span><span class="sxs-lookup"><span data-stu-id="bd241-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="bd241-109">Anzeigen von Daten in einem Diagramm.</span><span class="sxs-lookup"><span data-stu-id="bd241-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="bd241-110">So formatieren Sie Diagramme mit integrierten Designs.</span><span class="sxs-lookup"><span data-stu-id="bd241-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="bd241-111">Speichern von Diagrammen und Zwischenspeichern für eine bessere Leistung.</span><span class="sxs-lookup"><span data-stu-id="bd241-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="bd241-112">Dies sind die ASP.NET Programmierfunktionen, die in dem Artikel eingeführt werden:</span><span class="sxs-lookup"><span data-stu-id="bd241-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="bd241-113">Der `Chart` Helfer.</span><span class="sxs-lookup"><span data-stu-id="bd241-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="bd241-114">Die Informationen in diesem Artikel gelten für ASP.NET Webseiten 1.0 und Webseiten 2.</span><span class="sxs-lookup"><span data-stu-id="bd241-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="bd241-115">Der Chart-Helfer</span><span class="sxs-lookup"><span data-stu-id="bd241-115">The Chart Helper</span></span>

<span data-ttu-id="bd241-116">Wenn Sie Ihre Daten in grafischer Form `Chart` anzeigen möchten, können Sie den Helfer verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="bd241-117">Der `Chart` Helfer kann ein Bild rendern, das Daten in einer Vielzahl von Diagrammtypen anzeigt.</span><span class="sxs-lookup"><span data-stu-id="bd241-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="bd241-118">Es unterstützt viele Optionen für formatieren und beschriften.</span><span class="sxs-lookup"><span data-stu-id="bd241-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="bd241-119">Der `Chart` Helfer kann mehr als 30 Diagrammtypen rendern, einschließlich aller Diagrammtypen, die Sie aus Microsoft Excel oder anderen Werkzeugen kennen &#8212; Flächendiagramme, Balkendiagramme, Säulendiagramme, Liniendiagramme und Kreisdiagramme sowie spezialisiertere Diagramme wie Aktiendiagramme kennen.</span><span class="sxs-lookup"><span data-stu-id="bd241-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="bd241-120">**Flächendiagramm** ![Beschreibung: Bild des Flächendiagrammtyps](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="bd241-121">**Balkendiagramm** ![Beschreibung: Bild des Balkendiagrammtyps](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="bd241-122">**Säulendiagramm** ![Beschreibung: Bild des Säulendiagrammtyps](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="bd241-123">**Liniendiagramm** ![Beschreibung: Bild des Liniendiagrammtyps](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="bd241-124">**Kreisdiagramm** ![Beschreibung: Bild des Kreisdiagrammtyps](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="bd241-125">**Stockchart** ![Beschreibung: Bild des Aktiendiagrammtyps](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="bd241-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="bd241-126">Diagrammelemente</span><span class="sxs-lookup"><span data-stu-id="bd241-126">Chart Elements</span></span>

<span data-ttu-id="bd241-127">Diagramme zeigen Daten und zusätzliche Elemente wie Legenden, Achsen, Serien usw.</span><span class="sxs-lookup"><span data-stu-id="bd241-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="bd241-128">Die folgende Abbildung zeigt viele diagrammelemente, die Sie `Chart` anpassen können, wenn Sie den Helfer verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="bd241-129">In diesem Artikel erfahren Sie, wie Sie einige (nicht alle) dieser Elemente festlegen.</span><span class="sxs-lookup"><span data-stu-id="bd241-129">This article shows you how to set some (not all) of these elements.</span></span>

![Beschreibung: Bild mit den Diagrammelementen](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="bd241-131">Erstellen eines Diagramms aus Daten</span><span class="sxs-lookup"><span data-stu-id="bd241-131">Creating a Chart from Data</span></span>

<span data-ttu-id="bd241-132">Die Daten, die Sie in einem Diagramm anzeigen, können aus einem Array, aus den Ergebnissen stammen, die aus einer Datenbank zurückgegeben werden, oder aus Daten, die sich in einer XML-Datei befinden.</span><span class="sxs-lookup"><span data-stu-id="bd241-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="bd241-133">Verwenden eines Arrays</span><span class="sxs-lookup"><span data-stu-id="bd241-133">Using an Array</span></span>

<span data-ttu-id="bd241-134">Wie in [Einführung in ASP.NET Webseitenprogrammierung mit der Razor-Syntax](https://go.microsoft.com/fwlink/?LinkId=202890)erläutert, können Sie mit einem Array eine Sammlung ähnlicher Elemente in einer einzelnen Variablen speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="bd241-135">Sie können Arrays verwenden, um die Daten zu enthalten, die Sie in das Diagramm aufnehmen möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="bd241-136">Dieses Verfahren zeigt, wie Sie ein Diagramm aus Daten in Arrays mithilfe des Standarddiagrammtyps erstellen können.</span><span class="sxs-lookup"><span data-stu-id="bd241-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="bd241-137">Außerdem wird gezeigt, wie das Diagramm auf der Seite angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="bd241-138">Erstellen Sie eine neue Datei mit dem Namen *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="bd241-139">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="bd241-140">Der Code erstellt zuerst ein neues Diagramm und legt seine Breite und Höhe fest.</span><span class="sxs-lookup"><span data-stu-id="bd241-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="bd241-141">Sie geben den Diagrammtitel `AddTitle` mithilfe der Methode an.</span><span class="sxs-lookup"><span data-stu-id="bd241-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="bd241-142">Zum Hinzufügen von Daten `AddSeries` verwenden Sie die Methode.</span><span class="sxs-lookup"><span data-stu-id="bd241-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="bd241-143">In diesem Beispiel verwenden `name` `xValue`Sie `yValues` die Parameter `AddSeries` , und Parameter der Methode.</span><span class="sxs-lookup"><span data-stu-id="bd241-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="bd241-144">Der `name` Parameter wird in der Diagrammlegende angezeigt.</span><span class="sxs-lookup"><span data-stu-id="bd241-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="bd241-145">Der `xValue` Parameter enthält ein Array von Daten, das entlang der horizontalen Achse des Diagramms angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="bd241-146">Der `yValues` Parameter enthält ein Array von Daten, die zum Plotten der vertikalen Punkte des Diagramms verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="bd241-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="bd241-147">Die `Write` Methode rendert das Diagramm tatsächlich.</span><span class="sxs-lookup"><span data-stu-id="bd241-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="bd241-148">Da Sie in diesem Fall keinen Diagrammtyp `Chart` angegeben haben, rendert der Helfer das Standarddiagramm, das ein Säulendiagramm ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="bd241-149">Führen Sie die Seite im Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-149">Run the page in the browser.</span></span> <span data-ttu-id="bd241-150">Der Browser zeigt das Diagramm an.</span><span class="sxs-lookup"><span data-stu-id="bd241-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="bd241-151">Verwenden einer Datenbankabfrage für Diagrammdaten</span><span class="sxs-lookup"><span data-stu-id="bd241-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="bd241-152">Wenn sich die Informationen, die Sie planen möchten, in einer Datenbank befinden, können Sie eine Datenbankabfrage ausführen und dann Daten aus den Ergebnissen verwenden, um das Diagramm zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="bd241-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="bd241-153">Dieses Verfahren zeigt Ihnen, wie Sie die Daten aus der Datenbank lesen und anzeigen, die im Artikel Einführung in das [Arbeiten mit einer Datenbank in ASP.NET Webseitenwebsites](https://go.microsoft.com/fwlink/?LinkId=202893)erstellt wurden.</span><span class="sxs-lookup"><span data-stu-id="bd241-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="bd241-154">Fügen Sie dem Stammverzeichnis der Website einen *Ordner "App-Daten"\_* hinzu, wenn der Ordner noch nicht vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="bd241-155">Fügen Sie im Ordner *App-Daten\_* die Datenbankdatei mit dem Namen *SmallBakery.sdf* hinzu, die unter Einführung in das [Arbeiten mit einer Datenbank in ASP.NET Webseitenwebsites](https://go.microsoft.com/fwlink/?LinkId=202893)beschrieben wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="bd241-156">Erstellen Sie eine neue Datei mit dem Namen *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="bd241-157">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="bd241-158">Der Code öffnet zuerst die SmallBakery-Datenbank und `db`weist sie einer Variablen mit dem Namen zu.</span><span class="sxs-lookup"><span data-stu-id="bd241-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="bd241-159">Diese Variable `Database` stellt ein Objekt dar, das zum Lesen und Schreiben in die Datenbank verwendet werden kann.</span><span class="sxs-lookup"><span data-stu-id="bd241-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="bd241-160">Als Nächstes führt der Code eine SQL-Abfrage aus, um den Namen und den Preis jedes Produkts abzufragen.</span><span class="sxs-lookup"><span data-stu-id="bd241-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="bd241-161">Der Code erstellt ein neues Diagramm und übergibt die `DataBindTable` Datenbankabfrage an ihn, indem er die Methode des Diagramms aufruft.</span><span class="sxs-lookup"><span data-stu-id="bd241-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="bd241-162">Diese Methode verwendet zwei `dataSource` Parameter: Der Parameter ist für `xField` die Daten aus der Abfrage, und mit dem Parameter können Sie festlegen, welche Datenspalte für die x-Achse des Diagramms verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="bd241-163">Als Alternative zur `DataBindTable` Verwendung der Methode `AddSeries` können Sie `Chart` die Methode des Helfers verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="bd241-164">Mit `AddSeries` der Methode `xValue` können `yValues` Sie die und Parameter festlegen.</span><span class="sxs-lookup"><span data-stu-id="bd241-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="bd241-165">Anstatt die `DataBindTable` Methode beispielsweise wie folgt zu verwenden:</span><span class="sxs-lookup"><span data-stu-id="bd241-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="bd241-166">Sie können `AddSeries` die Methode wie folgt verwenden:</span><span class="sxs-lookup"><span data-stu-id="bd241-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="bd241-167">Beide führen die gleichen Ergebnisse aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-167">Both render the same results.</span></span> <span data-ttu-id="bd241-168">Die `AddSeries` Methode ist flexibler, da Sie den Diagrammtyp und `DataBindTable` die Daten expliziter angeben können, die Methode jedoch einfacher zu verwenden ist, wenn Sie die zusätzliche Flexibilität nicht benötigen.</span><span class="sxs-lookup"><span data-stu-id="bd241-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="bd241-169">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="bd241-170">Verwenden von XML-Daten</span><span class="sxs-lookup"><span data-stu-id="bd241-170">Using XML Data</span></span>

<span data-ttu-id="bd241-171">Die dritte Option für das Diagrammisten besteht darin, eine XML-Datei als Daten für das Diagramm zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="bd241-172">Dies erfordert, dass die XML-Datei auch über eine Schemadatei *(Xsd-Datei)* verfügt, die die XML-Struktur beschreibt.</span><span class="sxs-lookup"><span data-stu-id="bd241-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="bd241-173">Dieses Verfahren zeigt Ihnen, wie Sie Daten aus einer XML-Datei lesen.</span><span class="sxs-lookup"><span data-stu-id="bd241-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="bd241-174">Erstellen Sie im Ordner *App-Daten\_* eine neue XML-Datei mit dem Namen *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="bd241-175">Ersetzen Sie das vorhandene XML durch Folgendes, d. h. einige XML-Daten über Mitarbeiter in einem fiktiven Unternehmen.</span><span class="sxs-lookup"><span data-stu-id="bd241-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="bd241-176">Erstellen Sie im Ordner *App-Daten\_* eine neue XML-Datei mit dem Namen *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="bd241-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="bd241-177">(Beachten Sie, dass die Erweiterung dieses Mal *.xsd*.)</span><span class="sxs-lookup"><span data-stu-id="bd241-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="bd241-178">Ersetzen Sie die vorhandene XML-Datei durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="bd241-179">Erstellen Sie im Stammverzeichnis der Website eine neue Datei mit dem Namen *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="bd241-180">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="bd241-181">Der Code erstellt `DataSet` zuerst ein Objekt.</span><span class="sxs-lookup"><span data-stu-id="bd241-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="bd241-182">Dieses Objekt wird verwendet, um die aus der XML-Datei gelesenen Daten zu verwalten und entsprechend den Informationen in der Schemadatei zu organisieren.</span><span class="sxs-lookup"><span data-stu-id="bd241-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="bd241-183">(Beachten Sie, dass der obere `using SystemData`Rand des Codes die Anweisung enthält.</span><span class="sxs-lookup"><span data-stu-id="bd241-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="bd241-184">Dies ist erforderlich, um mit `DataSet` dem Objekt arbeiten zu können.</span><span class="sxs-lookup"><span data-stu-id="bd241-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="bd241-185">Weitere Informationen finden Sie unter [ &quot;Verwenden von&quot; Anweisungen und vollständig qualifizierten Namen](#SB_UsingStatements) weiter unten in diesem Artikel.)</span><span class="sxs-lookup"><span data-stu-id="bd241-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="bd241-186">Als Nächstes erstellt `DataView` der Code ein Objekt basierend auf dem Dataset.</span><span class="sxs-lookup"><span data-stu-id="bd241-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="bd241-187">Die Datenansicht stellt ein Objekt bereit, das das Diagramm an &#8212; das heißt, lesen und plotieren kann.</span><span class="sxs-lookup"><span data-stu-id="bd241-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="bd241-188">Das Diagramm bindet mithilfe der `AddSeries` Methode an die Daten, wie Sie zuvor beim `xValue` Diagrammieren der Arraydaten gesehen haben, mit der Ausnahme, dass dieses Mal die und `yValues` die Parameter auf das `DataView` Objekt festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="bd241-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="bd241-189">In diesem Beispiel wird auch gezeigt, wie Sie einen bestimmten Diagrammtyp angeben.</span><span class="sxs-lookup"><span data-stu-id="bd241-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="bd241-190">Wenn die Daten in `AddSeries` der `chartType` Methode hinzugefügt werden, wird der Parameter auch so eingestellt, dass ein Kreisdiagramm angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="bd241-191">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="bd241-192">"Verwenden" von Anweisungen und vollqualifizierten Namen</span><span class="sxs-lookup"><span data-stu-id="bd241-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="bd241-193">Das .NET Framework, auf dem ASP.NET Webseiten mit Razor-Syntax basiert, besteht aus vielen Tausenden von Komponenten (Klassen).</span><span class="sxs-lookup"><span data-stu-id="bd241-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="bd241-194">Damit es mit all diesen Klassen zu arbeiten ist, sind sie in *Namespaces*organisiert, die ein wenig wie Bibliotheken sind.</span><span class="sxs-lookup"><span data-stu-id="bd241-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="bd241-195">Der `System.Web` Namespace enthält beispielsweise Klassen, die die `System.Xml` Browser-/Serverkommunikation unterstützen, der Namespace enthält `System.Data` Klassen, die zum Erstellen und Lesen von XML-Dateien verwendet werden, und der Namespace enthält Klassen, mit denen Sie mit Daten arbeiten können.</span><span class="sxs-lookup"><span data-stu-id="bd241-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="bd241-196">Um auf eine bestimmte Klasse in .NET Framework zugreifen zu können, muss Code nicht nur den Klassennamen, sondern auch den Namespace kennen, in dem sich die Klasse befindet.</span><span class="sxs-lookup"><span data-stu-id="bd241-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="bd241-197">`Chart` Um den Helfer verwenden zu können, muss Code `System.Web.Helpers.Chart` beispielsweise die Klasse finden, die den Namespace (`System.Web.Helpers`) mit dem Klassennamen (`Chart`) kombiniert.</span><span class="sxs-lookup"><span data-stu-id="bd241-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="bd241-198">Dies wird als *vollständig qualifizierter* Name der Klasse bezeichnet, &#8212; ihre vollständige, eindeutige Position innerhalb der Weite von .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="bd241-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="bd241-199">Im Code würde dies wie folgt aussehen:</span><span class="sxs-lookup"><span data-stu-id="bd241-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="bd241-200">Es ist jedoch umständlich (und fehleranfällig), diese langen, vollqualifizierten Namen jedes Mal verwenden zu müssen, wenn Sie auf eine Klasse oder einen Helfer verweisen möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="bd241-201">Um die Verwendung von Klassennamen zu vereinfachen, können Sie daher die Namespaces *importieren,* die Sie interessieren.</span><span class="sxs-lookup"><span data-stu-id="bd241-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="bd241-202">Wenn Sie einen Namespace importiert haben, können Sie`Chart`nur einen Klassennamen`System.Web.Helpers.Chart`( ) anstelle des vollqualifizierten Namens ( ) verwenden.</span><span class="sxs-lookup"><span data-stu-id="bd241-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="bd241-203">Wenn Ihr Code ausgeführt wird und auf einen Klassennamen trifft, kann er nur in den Namensräumen suchen, die Sie importiert haben, um diese Klasse zu finden.</span><span class="sxs-lookup"><span data-stu-id="bd241-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="bd241-204">Wenn Sie ASP.NET Webseiten mit Razor-Syntax zum Erstellen von Webseiten verwenden, verwenden `WebPage` Sie in der Regel jedes Mal denselben Satz von Klassen, einschließlich der Klasse, der verschiedenen Hilfsprogramme usw.</span><span class="sxs-lookup"><span data-stu-id="bd241-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="bd241-205">Um Ihnen die Arbeit beim Importieren der relevanten Namespaces zu ersparen, wird bei jedem Erstellen einer Website ASP.NET so konfiguriert, dass automatisch eine Reihe von Kernnamespaces für jede Website importiert wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="bd241-206">Aus diesem Grund mussten Sie sich bisher nicht mit Namespaces oder Importen befassen. Alle Klassen, mit denen Sie gearbeitet haben, befinden sich in Namespaces, die bereits für Sie importiert wurden.</span><span class="sxs-lookup"><span data-stu-id="bd241-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="bd241-207">Manchmal müssen Sie jedoch mit einer Klasse arbeiten, die sich nicht in einem Namespace befindet, der automatisch für Sie importiert wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="bd241-208">In diesem Fall können Sie entweder den vollqualifizierten Namen dieser Klasse verwenden oder den Namespace, der die Klasse enthält, manuell importieren.</span><span class="sxs-lookup"><span data-stu-id="bd241-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="bd241-209">Zum Importieren eines Namespace `using` verwenden`import` Sie die Anweisung (in Visual Basic), wie Sie in einem Beispiel zuvor im Artikel gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="bd241-210">Die `DataSet` Klasse befindet sich `System.Data` z. B. im Namespace.</span><span class="sxs-lookup"><span data-stu-id="bd241-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="bd241-211">Der `System.Data` Namespace ist nicht automatisch für ASP.NET Razor-Seiten verfügbar.</span><span class="sxs-lookup"><span data-stu-id="bd241-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="bd241-212">Um mit der `DataSet` Klasse mit ihrem vollqualifizierten Namen zu arbeiten, können Sie daher Code wie folgt verwenden:</span><span class="sxs-lookup"><span data-stu-id="bd241-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="bd241-213">Wenn Sie die `DataSet` Klasse wiederholt verwenden müssen, können Sie einen Namespace wie diesen importieren und dann nur den Klassennamen im Code verwenden:</span><span class="sxs-lookup"><span data-stu-id="bd241-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="bd241-214">Sie können `using` Anweisungen für alle anderen .NET Framework-Namespaces hinzufügen, auf die Sie verweisen möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="bd241-215">Wie bereits erwähnt, müssen Sie dies jedoch nicht häufig tun, da sich die meisten Klassen, mit denen Sie arbeiten, in Namespaces befinden, die automatisch von ASP.NET für die Verwendung in *.cshtml-* und *VBhtml-Seiten* importiert werden.</span><span class="sxs-lookup"><span data-stu-id="bd241-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="bd241-216">Anzeigen von Diagrammen innerhalb einer Webseite</span><span class="sxs-lookup"><span data-stu-id="bd241-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="bd241-217">In den Beispielen, die Sie bisher gesehen haben, erstellen Sie ein Diagramm, und dann wird das Diagramm direkt im Browser als Grafik gerendert.</span><span class="sxs-lookup"><span data-stu-id="bd241-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="bd241-218">In vielen Fällen möchten Sie jedoch ein Diagramm als Teil einer Seite anzeigen, nicht nur von selbst im Browser.</span><span class="sxs-lookup"><span data-stu-id="bd241-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="bd241-219">Dazu ist ein zweistufiger Prozess erforderlich.</span><span class="sxs-lookup"><span data-stu-id="bd241-219">To do that requires a two-step process.</span></span> <span data-ttu-id="bd241-220">Der erste Schritt besteht darin, eine Seite zu erstellen, die das Diagramm generiert, wie Sie bereits gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="bd241-221">Der zweite Schritt besteht darin, das resultierende Bild auf einer anderen Seite anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="bd241-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="bd241-222">Um das Bild anzuzeigen, `<img>` verwenden Sie ein HTML-Element auf die gleiche Weise, wie Sie jedes Bild anzeigen würden.</span><span class="sxs-lookup"><span data-stu-id="bd241-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="bd241-223">Anstatt jedoch auf eine *.jpg-* oder *.png-Datei* zu verweisen, verweist das `<img>` Element auf die *.cshtml-Datei,* die den `Chart` Helfer enthält, der das Diagramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="bd241-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="bd241-224">Wenn die Anzeigeseite `<img>` ausgeführt wird, ruft `Chart` das Element die Ausgabe des Helfers ab und rendert das Diagramm.</span><span class="sxs-lookup"><span data-stu-id="bd241-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="bd241-225">Erstellen Sie eine Datei mit dem Namen *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="bd241-226">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="bd241-227">Der Code `<img>` verwendet das Element, um das Diagramm anzuzeigen, das Sie zuvor in der Datei *ChartArrayBasic.cshtml* erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="bd241-228">Führen Sie die Webseite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-228">Run the web page in a browser.</span></span> <span data-ttu-id="bd241-229">Die Datei *ShowChart.cshtml* zeigt das Diagrammbild basierend auf dem Code an, der in der Datei *ChartArrayBasic.cshtml* enthalten ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="bd241-230">Styling eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="bd241-230">Styling a Chart</span></span>

<span data-ttu-id="bd241-231">Der `Chart` Helfer unterstützt eine große Anzahl von Optionen, mit denen Sie die Darstellung des Diagramms anpassen können.</span><span class="sxs-lookup"><span data-stu-id="bd241-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="bd241-232">Sie können Farben, Schriftarten, Rahmen usw. festlegen.</span><span class="sxs-lookup"><span data-stu-id="bd241-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="bd241-233">Eine einfache Möglichkeit, die Darstellung eines Diagramms anzupassen, ist die Verwendung eines *Themas*.</span><span class="sxs-lookup"><span data-stu-id="bd241-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="bd241-234">Designs sind Informationssammlungen, die angeben, wie ein Diagramm mithilfe von Schriftarten, Farben, Beschriftungen, Paletten, Rahmen und Effekten gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="bd241-235">(Beachten Sie, dass der Stil eines Diagramms nicht den Diagrammtyp angibt.)</span><span class="sxs-lookup"><span data-stu-id="bd241-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="bd241-236">In der folgenden Tabelle sind integrierte Designs aufgeführt.</span><span class="sxs-lookup"><span data-stu-id="bd241-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="bd241-237">Design</span><span class="sxs-lookup"><span data-stu-id="bd241-237">Theme</span></span> | <span data-ttu-id="bd241-238">BESCHREIBUNG</span><span class="sxs-lookup"><span data-stu-id="bd241-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="bd241-239">Zeigt rote Spalten auf weißem Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="bd241-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="bd241-240">Zeigt blaue Spalten auf einem blauen Farbverlaufshintergrund an.</span><span class="sxs-lookup"><span data-stu-id="bd241-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="bd241-241">Zeigt blaue Spalten auf einem grünen Farbverlaufshintergrund an.</span><span class="sxs-lookup"><span data-stu-id="bd241-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="bd241-242">Zeigt orangefarbene Spalten auf einem gelben Farbverlaufshintergrund an.</span><span class="sxs-lookup"><span data-stu-id="bd241-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="bd241-243">Zeigt rote 3D-Spalten auf weißem Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="bd241-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="bd241-244">Sie können das Design angeben, das beim Erstellen eines neuen Diagramms verwendet werden soll.</span><span class="sxs-lookup"><span data-stu-id="bd241-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="bd241-245">Erstellen Sie eine neue Datei mit dem Namen *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="bd241-246">Ersetzen Sie den vorhandenen Inhalt auf der Seite durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="bd241-247">Dieser Code ist derselbe wie das frühere Beispiel, das `theme` die Datenbank für `Chart` Daten verwendet, aber den Parameter hinzufügt, wenn er das Objekt erstellt.</span><span class="sxs-lookup"><span data-stu-id="bd241-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="bd241-248">Im Folgenden wird der geänderte Code angezeigt:</span><span class="sxs-lookup"><span data-stu-id="bd241-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="bd241-249">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-249">Run the page in a browser.</span></span> <span data-ttu-id="bd241-250">Sie sehen die gleichen Daten wie zuvor, aber das Diagramm sieht polierter aus:</span><span class="sxs-lookup"><span data-stu-id="bd241-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="bd241-251">Speichern eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="bd241-251">Saving a Chart</span></span>

<span data-ttu-id="bd241-252">Wenn Sie `Chart` den Helfer verwenden, wie Sie es bisher in diesem Artikel gesehen haben, erstellt der Helfer das Diagramm bei jedem Aufruf von Grund auf neu.</span><span class="sxs-lookup"><span data-stu-id="bd241-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="bd241-253">Bei Bedarf fragt der Code für das Diagramm auch die Datenbank erneut ab oder liest die XML-Datei erneut, um die Daten abzurufen.</span><span class="sxs-lookup"><span data-stu-id="bd241-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="bd241-254">In einigen Fällen kann dies ein komplexer Vorgang sein, z. B. wenn die Datenbank, die Sie abfragen, groß ist oder wenn die XML-Datei viele Daten enthält.</span><span class="sxs-lookup"><span data-stu-id="bd241-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="bd241-255">Auch wenn das Diagramm nicht viele Daten enthält, erfordert das dynamische Erstellen eines Bildes Serverressourcen, und wenn viele Personen die Seite oder seiten anfordern, die das Diagramm anzeigen, kann dies Auswirkungen auf die Leistung Ihrer Website haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="bd241-256">Um die potenziellen Auswirkungen des Erstellens eines Diagramms auf die Leistung zu verringern, können Sie ein Diagramm erstellen, wenn Sie es zum ersten Mal benötigen, und es dann speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="bd241-257">Wenn das Diagramm erneut benötigt wird, anstatt es zu regenerieren, können Sie einfach die gespeicherte Version abrufen und rendern.</span><span class="sxs-lookup"><span data-stu-id="bd241-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="bd241-258">Sie können ein Diagramm auf folgende Weise speichern:</span><span class="sxs-lookup"><span data-stu-id="bd241-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="bd241-259">Zwischenspeichern Sie das Diagramm im Computerspeicher (auf dem Server).</span><span class="sxs-lookup"><span data-stu-id="bd241-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="bd241-260">Speichern Sie das Diagramm als Bilddatei.</span><span class="sxs-lookup"><span data-stu-id="bd241-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="bd241-261">Speichern Sie das Diagramm als XML-Datei.</span><span class="sxs-lookup"><span data-stu-id="bd241-261">Save the chart as an XML file.</span></span> <span data-ttu-id="bd241-262">Mit dieser Option können Sie das Diagramm ändern, bevor Sie es speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="bd241-263">Zwischenspeichern eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="bd241-263">Caching a Chart</span></span>

<span data-ttu-id="bd241-264">Nachdem Sie ein Diagramm erstellt haben, können Sie es zwischenspeichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="bd241-265">Das Zwischenspeichern eines Diagramms bedeutet, dass es nicht neu erstellt werden muss, wenn es erneut angezeigt werden muss.</span><span class="sxs-lookup"><span data-stu-id="bd241-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="bd241-266">Wenn Sie ein Diagramm im Cache speichern, geben Sie ihm einen Schlüssel, der für dieses Diagramm eindeutig sein muss.</span><span class="sxs-lookup"><span data-stu-id="bd241-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="bd241-267">Im Cache gespeicherte Diagramme werden möglicherweise entfernt, wenn auf dem Server nicht mehr arbeitsspeicherabhängig ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="bd241-268">Darüber hinaus wird der Cache gelöscht, wenn die Anwendung aus irgendeinem Grund neu gestartet wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="bd241-269">Daher besteht die Standardmethode, mit einem zwischengespeicherten Diagramm zu arbeiten, darin, immer zuerst zu überprüfen, ob es im Cache verfügbar ist, und wenn nicht, und dann zu erstellen oder neu zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="bd241-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="bd241-270">Erstellen Sie im Stammverzeichnis Ihrer Website eine Datei mit dem Namen *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="bd241-271">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="bd241-272">Das `<img>` Tag `src` enthält ein Attribut, das auf die Datei *ChartSaveToCache.cshtml* verweist und einen Schlüssel als Abfragezeichenfolge an die Seite übergibt.</span><span class="sxs-lookup"><span data-stu-id="bd241-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="bd241-273">Der Schlüssel enthält &quot;den&quot;Wert myChartKey .</span><span class="sxs-lookup"><span data-stu-id="bd241-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="bd241-274">Die Datei *ChartSaveToCache.cshtml* enthält den `Chart` Helfer, der das Diagramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="bd241-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="bd241-275">Sie erstellen diese Seite gleich.</span><span class="sxs-lookup"><span data-stu-id="bd241-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="bd241-276">Am Ende der Seite befindet sich ein Link zu einer Seite mit dem Namen *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="bd241-277">Das ist eine Seite, die Sie auch in Kürze erstellen werden.</span><span class="sxs-lookup"><span data-stu-id="bd241-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="bd241-278">Sie benötigen *ClearCache.cshtml* nur, um die Zwischenspeicherung für dieses Beispiel zu testen – es ist kein Link oder eine Seite, die Sie normalerweise bei der Arbeit mit zwischengespeicherten Diagrammen einschließen würden.</span><span class="sxs-lookup"><span data-stu-id="bd241-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="bd241-279">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="bd241-280">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="bd241-281">Der Code überprüft zunächst, ob etwas als Schlüsselwert in der Abfragezeichenfolge übergeben wurde.</span><span class="sxs-lookup"><span data-stu-id="bd241-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="bd241-282">Wenn dies der Fall ist, versucht der Code, ein Diagramm aus dem Cache zu lesen, indem er die `GetFromCache` Methode aufruft und den Schlüssel übergibt.</span><span class="sxs-lookup"><span data-stu-id="bd241-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="bd241-283">Wenn sich herausstellt, dass sich unter diesem Schlüssel nichts im Cache befindet (was beim ersten Abfragen des Diagramms passieren würde), erstellt der Code das Diagramm wie gewohnt.</span><span class="sxs-lookup"><span data-stu-id="bd241-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="bd241-284">Wenn das Diagramm fertig ist, speichert der `SaveToCache`Code es im Cache, indem er aufruft.</span><span class="sxs-lookup"><span data-stu-id="bd241-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="bd241-285">Diese Methode erfordert einen Schlüssel (damit das Diagramm später angefordert werden kann) und die Zeit, die das Diagramm im Cache speichern soll.</span><span class="sxs-lookup"><span data-stu-id="bd241-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="bd241-286">(Die genaue Zeit, zu der Sie ein Diagramm zwischenspeichern würden, hängt davon ab, wie oft sich die Daten ändern könnten.) Die `SaveToCache` Methode erfordert `slidingExpiration` auch einen Parameter&#8212; wenn dieser wert ist, wird der Timeoutzähler bei jedem Zugriff auf das Diagramm zurückgesetzt.</span><span class="sxs-lookup"><span data-stu-id="bd241-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="bd241-287">In diesem Fall bedeutet dies, dass der Cacheeintrag des Diagramms 2 Minuten nach dem letzten Zugriff einer Person auf das Diagramm abläuft.</span><span class="sxs-lookup"><span data-stu-id="bd241-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="bd241-288">(Die Alternative zum gleitenden Ablauf ist der absolute Ablauf, d. h., der Cacheeintrag würde genau 2 Minuten nach seiner Einlage in den Cache ablaufen, unabhängig davon, wie oft darauf zugegriffen wurde.)</span><span class="sxs-lookup"><span data-stu-id="bd241-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="bd241-289">Schließlich verwendet der `WriteFromCache` Code die Methode zum Abrufen und Rendern des Diagramms aus dem Cache.</span><span class="sxs-lookup"><span data-stu-id="bd241-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="bd241-290">Beachten Sie, dass `if` sich diese Methode außerhalb des Blocks befindet, der den Cache überprüft, da das Diagramm aus dem Cache abholt, unabhängig davon, ob das Diagramm zunächst vorhanden war oder generiert und im Cache gespeichert werden musste.</span><span class="sxs-lookup"><span data-stu-id="bd241-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="bd241-291">Beachten Sie, dass `AddTitle` die Methode im Beispiel einen Zeitstempel enthält.</span><span class="sxs-lookup"><span data-stu-id="bd241-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="bd241-292">(Es fügt das aktuelle `DateTime.Now` Datum und die aktuelle Uhrzeit &#8212; &#8212; zum Titel hinzu.)</span><span class="sxs-lookup"><span data-stu-id="bd241-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="bd241-293">Erstellen Sie eine neue Seite mit dem Namen *ClearCache.cshtml,* und ersetzen Sie ihren Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="bd241-294">Diese Seite `WebCache` verwendet den Helfer, um das Diagramm zu entfernen, das in *ChartSaveToCache.cshtml*zwischengespeichert ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="bd241-295">Wie bereits erwähnt, müssen Sie normalerweise keine solche Seite haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="bd241-296">Sie erstellen es hier nur, um das Zwischenspeichern zu vereinfachen.</span><span class="sxs-lookup"><span data-stu-id="bd241-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="bd241-297">Führen Sie die Webseite *ShowCachedChart.cshtml* in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="bd241-298">Auf der Seite wird das Diagrammbild basierend auf dem Code angezeigt, der in der Datei *ChartSaveToCache.cshtml* enthalten ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="bd241-299">Beachten Sie, was der Zeitstempel im Diagrammtitel sagt.</span><span class="sxs-lookup"><span data-stu-id="bd241-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Beschreibung: Bild des Basisdiagramms mit Zeitstempel im Diagrammtitel](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="bd241-301">Schließen Sie den Browser.</span><span class="sxs-lookup"><span data-stu-id="bd241-301">Close the browser.</span></span>
8. <span data-ttu-id="bd241-302">Führen Sie *ShowCachedChart.cshtml* erneut aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="bd241-303">Beachten Sie, dass der Zeitstempel derselbe ist wie zuvor, was darauf hinweist, dass das Diagramm nicht regeneriert wurde, sondern aus dem Cache gelesen wurde.</span><span class="sxs-lookup"><span data-stu-id="bd241-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="bd241-304">Klicken Sie in *ShowCachedChart.cshtml*auf den Link **Cache löschen.**</span><span class="sxs-lookup"><span data-stu-id="bd241-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="bd241-305">Dadurch werden Sie zu *ClearCache.cshtml*, das meldet, dass der Cache gelöscht wurde.</span><span class="sxs-lookup"><span data-stu-id="bd241-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="bd241-306">Klicken Sie auf den Link **Zurück zu ShowCachedChart.cshtml,** oder führen Sie *ShowCachedChart.cshtml* von WebMatrix erneut aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="bd241-307">Beachten Sie, dass sich der Zeitstempel dieses Mal geändert hat, da der Cache gelöscht wurde.</span><span class="sxs-lookup"><span data-stu-id="bd241-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="bd241-308">Daher musste der Code das Diagramm regenerieren und wieder in den Cache speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="bd241-309">Speichern eines Diagramms als Bilddatei</span><span class="sxs-lookup"><span data-stu-id="bd241-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="bd241-310">Sie können ein Diagramm auch als Bilddatei (z. B. als *JPG-Datei)* auf dem Server speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="bd241-311">Sie können dann die Bilddatei so verwenden, wie Sie es bei jedem Bild tun würden.</span><span class="sxs-lookup"><span data-stu-id="bd241-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="bd241-312">Der Vorteil ist, dass die Datei gespeichert und nicht in einem temporären Cache gespeichert wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="bd241-313">Sie können ein neues Diagrammbild zu unterschiedlichen Zeiten (z. B. stündlich) speichern und dann die Änderungen, die im Laufe der Zeit auftreten, permanent aufzeichnen.</span><span class="sxs-lookup"><span data-stu-id="bd241-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="bd241-314">Beachten Sie, dass Sie sicherstellen müssen, dass Ihre Webanwendung über die Berechtigung zum Speichern einer Datei in dem Ordner auf dem Server verfügt, auf dem Sie die Bilddatei absetzen möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="bd241-315">Erstellen Sie im Stammverzeichnis Ihrer Website einen Ordner mit dem Namen \* \_ChartFiles,\* falls dieser noch nicht vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="bd241-316">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="bd241-317">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="bd241-318">Der Code überprüft zuerst, ob die *JPG-Datei* vorhanden ist, indem die Methode aufgerufen wird. `File.Exists`</span><span class="sxs-lookup"><span data-stu-id="bd241-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="bd241-319">Wenn die Datei nicht vorhanden ist, `Chart` erstellt der Code ein neues aus einem Array.</span><span class="sxs-lookup"><span data-stu-id="bd241-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="bd241-320">Diesmal ruft der Code `Save` die Methode `path` auf und übergibt den Parameter, um den Dateipfad und den Dateinamen anzugeben, wo das Diagramm gespeichert werden soll.</span><span class="sxs-lookup"><span data-stu-id="bd241-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="bd241-321">Im Textkörper der Seite `<img>` verwendet ein Element den Pfad, um auf die anzuzeigende *JPG-Datei* zu verweisen.</span><span class="sxs-lookup"><span data-stu-id="bd241-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="bd241-322">Führen Sie die Datei *ChartSave.cshtml* aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="bd241-323">Zurück zu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="bd241-323">Return to WebMatrix.</span></span> <span data-ttu-id="bd241-324">Beachten Sie, dass eine Bilddatei mit dem Namen *chart01.jpg* im \* \_\* Ordner ChartFiles gespeichert wurde.</span><span class="sxs-lookup"><span data-stu-id="bd241-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="bd241-325">Speichern eines Diagramms als XML-Datei</span><span class="sxs-lookup"><span data-stu-id="bd241-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="bd241-326">Schließlich können Sie ein Diagramm als XML-Datei auf dem Server speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="bd241-327">Ein Vorteil der Verwendung dieser Methode gegenüber dem Zwischenspeichern des Diagramms oder dem Speichern des Diagramms in einer Datei besteht darin, dass Sie den XML-Code ändern können, bevor Sie das Diagramm anzeigen, wenn Sie möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="bd241-328">Ihre Anwendung muss über Lese-/Schreibberechtigungen für den Ordner auf dem Server verfügen, auf dem Sie die Bilddatei ablegen möchten.</span><span class="sxs-lookup"><span data-stu-id="bd241-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="bd241-329">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bd241-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="bd241-330">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="bd241-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="bd241-331">Dieser Code ähnelt dem Code, den Sie zuvor zum Speichern eines Diagramms im Cache gesehen haben, mit der Ausnahme, dass eine XML-Datei verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="bd241-332">Der Code überprüft zunächst, ob die XML-Datei vorhanden ist, indem die Methode aufgerufen wird. `File.Exists`</span><span class="sxs-lookup"><span data-stu-id="bd241-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="bd241-333">Wenn die Datei vorhanden ist, `Chart` erstellt der Code ein `themePath` neues Objekt und übergibt den Dateinamen als Parameter.</span><span class="sxs-lookup"><span data-stu-id="bd241-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="bd241-334">Dadurch wird das Diagramm basierend auf dem in der XML-Datei gespeicherten Diagramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="bd241-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="bd241-335">Wenn die XML-Datei noch nicht vorhanden ist, erstellt der `SaveXml` Code ein Diagramm wie normal und ruft es dann auf, um es zu speichern.</span><span class="sxs-lookup"><span data-stu-id="bd241-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="bd241-336">Das Diagramm wird mit `Write` der Methode gerendert, wie Sie zuvor gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="bd241-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="bd241-337">Wie bei der Seite, die zwischenspeichern zeigte, enthält dieser Code einen Zeitstempel im Diagrammtitel.</span><span class="sxs-lookup"><span data-stu-id="bd241-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="bd241-338">Erstellen Sie eine neue Seite mit dem Namen *ChartDisplayXMLChart.cshtml,* und fügen Sie ihr das folgende Markup hinzu:</span><span class="sxs-lookup"><span data-stu-id="bd241-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="bd241-339">Führen Sie die Seite *ChartDisplayXMLChart.cshtml* aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="bd241-340">Das Diagramm wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="bd241-340">The chart is displayed.</span></span> <span data-ttu-id="bd241-341">Beachten Sie den Zeitstempel im Titel des Diagramms.</span><span class="sxs-lookup"><span data-stu-id="bd241-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="bd241-342">Schließen Sie den Browser.</span><span class="sxs-lookup"><span data-stu-id="bd241-342">Close the browser.</span></span>
6. <span data-ttu-id="bd241-343">Klicken Sie in WebMatrix mit der **Refresh**rechten Maustaste auf den \* \_Ordner ChartFiles,\* klicken Sie auf Aktualisieren , und öffnen Sie dann den Ordner.</span><span class="sxs-lookup"><span data-stu-id="bd241-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="bd241-344">Die Datei *XMLChart.xml* in diesem `Chart` Ordner wurde vom Helfer erstellt.</span><span class="sxs-lookup"><span data-stu-id="bd241-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Beschreibung: Der _ChartFiles Ordner mit der Datei XMLChart.xml, die vom Chart-Helfer erstellt wurde.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="bd241-346">Führen Sie die Seite *ChartDisplayXMLChart.cshtml* erneut aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="bd241-347">Das Diagramm zeigt den gleichen Zeitstempel wie beim ersten Führte der Seite.</span><span class="sxs-lookup"><span data-stu-id="bd241-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="bd241-348">Das liegt daran, dass das Diagramm aus dem zuvor gespeicherten XML generiert wird.</span><span class="sxs-lookup"><span data-stu-id="bd241-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="bd241-349">Öffnen Sie in \* \_\* WebMatrix den Ordner ChartFiles, und löschen Sie die Datei *XMLChart.xml.*</span><span class="sxs-lookup"><span data-stu-id="bd241-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="bd241-350">Führen Sie die Seite *ChartDisplayXMLChart.cshtml* erneut aus.</span><span class="sxs-lookup"><span data-stu-id="bd241-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="bd241-351">Diesmal wird der Zeitstempel aktualisiert, `Chart` da der Helfer die XML-Datei neu erstellen musste.</span><span class="sxs-lookup"><span data-stu-id="bd241-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="bd241-352">Wenn Sie möchten, überprüfen Sie den \* \_Ordner ChartFiles,\* und beachten Sie, dass die XML-Datei wieder vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="bd241-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="bd241-353">Weitere Ressourcen</span><span class="sxs-lookup"><span data-stu-id="bd241-353">Additional Resources</span></span>

- [<span data-ttu-id="bd241-354">Einführung in das Arbeiten mit einer Datenbank in ASP.NET Webseiten-Websites</span><span class="sxs-lookup"><span data-stu-id="bd241-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="bd241-355">Verwenden von Caching in ASP.NET Webseiten-Websites zur Verbesserung der Leistung</span><span class="sxs-lookup"><span data-stu-id="bd241-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="bd241-356">[Diagrammklasse](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Webseiten-API-Referenz auf MSDN)</span><span class="sxs-lookup"><span data-stu-id="bd241-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
