---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Einführung ASP.NET Webseiten - HTML Form Basics | Microsoft Docs
author: Rick-Anderson
description: In diesem Tutorial werden die Grundlagen zum Erstellen eines Eingabeformulars und zum Behandeln der Benutzereingaben bei verwendungsASP.NET Webseiten (Razor) erläutert. Und jetzt, da Sie...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/06/2020
ms.locfileid: "80675922"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="6cf9a-104">Einführung in ASP.NET Webseiten - HTML Form Basics</span><span class="sxs-lookup"><span data-stu-id="6cf9a-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="6cf9a-105"> von [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="6cf9a-106">In diesem Tutorial werden die Grundlagen zum Erstellen eines Eingabeformulars und zum Behandeln der Benutzereingaben bei verwendungsASP.NET Webseiten (Razor) erläutert.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="6cf9a-107">Und jetzt, da Sie über eine Datenbank verfügen, verwenden Sie Ihre Formularkenntnisse, damit Benutzer bestimmte Filme in der Datenbank finden können.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="6cf9a-108">Es wird davon ausgegangen, dass Sie die Reihe durch [Einführung in die Anzeige von Daten mit ASP.NET Webseiten](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)abgeschlossen haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="6cf9a-109">Sie lernen Folgendes:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="6cf9a-110">Erstellen eines Formulars mithilfe von Standard-HTML-Elementen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="6cf9a-111">So lesen Sie die Eingabe des Benutzers in einem Formular.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="6cf9a-112">Erstellen einer SQL-Abfrage, die mithilfe eines vom Benutzer bereitstellten Suchbegriffs selektiv Daten abruft.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="6cf9a-113">Wie man Felder auf der Seite "Erinnern" hat, was der Benutzer eingegeben hat.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="6cf9a-114">Diskutierte Features/Technologien:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="6cf9a-115">Das `Request`-Objekt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-115">The `Request` object.</span></span>
> - <span data-ttu-id="6cf9a-116">Die `Where` SQL-Klausel.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="6cf9a-117">Sie lernen Folgendes</span><span class="sxs-lookup"><span data-stu-id="6cf9a-117">What You'll Build</span></span>

<span data-ttu-id="6cf9a-118">Im vorherigen Lernprogramm haben Sie eine Datenbank erstellt, ihr `WebGrid` Daten hinzugefügt und dann den Helfer zum Anzeigen der Daten verwendet.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="6cf9a-119">In diesem Tutorial fügen Sie ein Suchfeld hinzu, in dem Sie Filme eines bestimmten Genres suchen können oder deren Titel das eingegebene Wort enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="6cf9a-120">(Sie können z. B. alle Filme finden, deren Genre "Action" lautet oder deren Titel "Harry" oder "Abenteuer" enthält.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="6cf9a-121">Wenn Sie mit diesem Tutorial fertig sind, haben Sie eine Seite wie diese:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Filmseite mit Genre- und Titelsuche](form-basics/_static/image1.png)

<span data-ttu-id="6cf9a-123">Der Auflistungsteil der Seite ist derselbe &mdash; wie im letzten Tutorial ein Raster.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="6cf9a-124">Der Unterschied besteht darin, dass das Raster nur die Filme anzeigt, nach denen Sie gesucht haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="6cf9a-125">Informationen zu HTML-Formularen</span><span class="sxs-lookup"><span data-stu-id="6cf9a-125">About HTML Forms</span></span>

<span data-ttu-id="6cf9a-126">(Wenn Sie Erfahrung mit dem Erstellen von HTML-Formularen und mit dem Unterschied zwischen `GET` und `POST`haben, können Sie diesen Abschnitt überspringen.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="6cf9a-127">Ein Formular enthält &mdash; Textfelder, Schaltflächen, Optionsfelder, Kontrollkästchen, Dropdownlisten usw.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="6cf9a-128">Benutzer füllen diese Steuerelemente aus oder treffen eine Auswahl, und senden dann das Formular, indem Sie auf eine Schaltfläche klicken.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="6cf9a-129">Die grundlegende HTML-Syntax eines Formulars wird in diesem Beispiel veranschaulicht:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="6cf9a-130">Wenn dieses Markup auf einer Seite ausgeführt wird, wird ein einfaches Formular erstellt, das wie folgt aussieht:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Grundlegendes HTML-Formular als im Browser gerendert](form-basics/_static/image2.png)

<span data-ttu-id="6cf9a-132">Das `<form>` Element umschließt HTML-Elemente, die übermittelt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="6cf9a-133">(Ein einfacher Fehler ist, Elemente zur Seite hinzuzufügen, aber `<form>` dann vergessen, sie in einem Element zu setzen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="6cf9a-134">In diesem Fall wird nichts eingereicht.) Das `method` Attribut teilt dem Browser mit, wie die Benutzereingabe übermittelt wird.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="6cf9a-135">Sie legen `post` dies auf festzulegen, wenn Sie `get` eine Aktualisierung auf dem Server durchführen oder wenn Sie nur Daten vom Server abrufen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="6cf9a-136">**GET-, POST- und HTTP-Verbsicherheit**</span><span class="sxs-lookup"><span data-stu-id="6cf9a-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="6cf9a-137">HTTP, das Protokoll, das Browser und Server zum Informationsaustausch verwenden, ist in seinen grundlegenden Vorgängen bemerkenswert einfach.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="6cf9a-138">Browser verwenden nur wenige Verben, um Anforderungen an Server zu stellen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="6cf9a-139">Wenn Sie Code für das Web schreiben, ist es hilfreich, diese Verben zu verstehen und wie Browser und Server sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="6cf9a-140">Bei weitem die am häufigsten verwendeten Verben sind diese:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="6cf9a-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-141">`GET`.</span></span> <span data-ttu-id="6cf9a-142">Der Browser verwendet dieses Verb, um etwas vom Server abzurufen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="6cf9a-143">Wenn Sie beispielsweise eine URL in Ihren Browser `GET` eingeben, führt der Browser einen Vorgang aus, um die gewünschte Seite anzufordern.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="6cf9a-144">Wenn die Seite Grafiken enthält, `GET` führt der Browser zusätzliche Vorgänge aus, um die Bilder abzubekommen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="6cf9a-145">Wenn `GET` der Vorgang Informationen an den Server übergeben muss, werden die Informationen als Teil der URL in der Abfragezeichenfolge übergeben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="6cf9a-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-146">`POST`.</span></span> <span data-ttu-id="6cf9a-147">Der Browser `POST` sendet eine Anforderung, um Daten zu übermitteln, die auf dem Server hinzugefügt oder geändert werden sollen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="6cf9a-148">Das `POST` Verb wird z. B. verwendet, um Datensätze in einer Datenbank zu erstellen oder vorhandene zu ändern.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="6cf9a-149">Wenn Sie in den meisten Fall ein Formular ausfüllen und auf `POST` die Schaltfläche Senden klicken, führt der Browser einen Vorgang aus.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="6cf9a-150">Bei `POST` einem Vorgang befinden sich die an den Server übergebenen Daten im Hauptteil der Seite.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="6cf9a-151">Ein wichtiger Unterschied zwischen diesen `GET` Verben besteht darin, dass ein Vorgang nichts auf dem Server ändern `GET` soll – oder um es etwas abstrakter zu sagen, dass ein Vorgang nicht zu einer Änderung des Zustands auf dem Server führt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="6cf9a-152">Sie können `GET` einen Vorgang für dieselben Ressourcen begehen, so oft Sie möchten, und diese Ressourcen ändern sich nicht.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="6cf9a-153">(Eine `GET` Operation wird oft als "sicher" bezeichnet, oder um einen technischen Begriff zu verwenden, ist *idempotent*.) Im Gegensatz dazu ändert `POST` eine Anforderung natürlich bei jeder Ausführung des Vorgangs etwas auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="6cf9a-154">Zwei Beispiele werden dazu beitragen, diese Unterscheidung zu veranschaulichen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="6cf9a-155">Wenn Sie eine Suche mit einer Engine wie Bing oder Google durchführen, füllen Sie ein Formular aus, das aus einem Textfeld besteht, und klicken dann auf die Suchschaltfläche.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="6cf9a-156">Der Browser `GET` führt einen Vorgang aus, wobei der Wert, den Sie in das Feld eingegeben haben, als Teil der URL übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="6cf9a-157">Die `GET` Verwendung eines Vorgangs für diesen Formulartyp ist in Ordnung, da ein Suchvorgang keine Ressourcen auf dem Server ändert, sondern nur Informationen abruft.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="6cf9a-158">Betrachten Sie nun den Prozess der Online-Bestellung.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="6cf9a-159">Sie geben die Bestelldetails ein und klicken dann auf die Schaltfläche Absenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="6cf9a-160">Dieser Vorgang ist `POST` eine Anforderung, da der Vorgang zu Änderungen auf dem Server führt, z. B. zu einem neuen Auftragsdatensatz, einer Änderung ihrer Kontoinformationen und möglicherweise vielen anderen Änderungen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="6cf9a-161">Im `GET` Gegensatz zum Vorgang `POST` können Sie Ihre Anforderung nicht wiederholen– wenn Sie dieanforderung erneut übermittelt haben, würden Sie eine neue Bestellung auf dem Server generieren.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="6cf9a-162">(In solchen Fällen warnen Websites Sie oft davor, mehr als einmal auf eine Schaltfläche zum Absenden zu klicken, oder deaktivieren die Schaltfläche "Absenden", sodass Sie das Formular nicht versehentlich erneut senden.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="6cf9a-163">Im Verlauf dieses Tutorials verwenden Sie `GET` sowohl einen `POST` Vorgang als auch einen Vorgang, um mit HTML-Formularen zu arbeiten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="6cf9a-164">Wir erklären in jedem Fall, warum das Verb, das Sie verwenden, das richtige ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="6cf9a-165">(Weitere Informationen zu HTTP-Verben finden Sie im Artikel [Methodendefinitionen](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) auf der W3C-Website.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="6cf9a-166">Die meisten Benutzereingabeelemente sind HTML-Elemente. `<input>`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="6cf9a-167">Sie sehen `<input type="type" name="name">,` so aus, als ob *der Typ* die Art des gewünschten Benutzereingabesteuerelements angibt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="6cf9a-168">Diese Elemente sind die gemeinsamen:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-168">These elements are the common ones:</span></span>

- <span data-ttu-id="6cf9a-169">Textfeld:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="6cf9a-170">Kontrollkästchen:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="6cf9a-171">Optionsfeld:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="6cf9a-172">Schaltfläche:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="6cf9a-173">Schaltfläche "Senden":`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="6cf9a-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="6cf9a-174">Sie können das `<textarea>` Element auch verwenden, um `<select>` ein mehrzeiliges Textfeld zu erstellen, und das Element, um eine Dropdown- oder Scrollliste zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="6cf9a-175">(Weitere Informationen zu HTML-Formularelementen finden Sie unter [HTML-Formulare und Eingaben](http://www.w3schools.com/html/html_forms.asp) auf der W3Schools-Website.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="6cf9a-176">Das `name` Attribut ist sehr wichtig, da der Name ist, wie Sie den Wert des Elements später erhalten, wie Sie in Kürze sehen werden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="6cf9a-177">Der interessante Teil ist, was Sie, der Seitenentwickler, mit den Eingaben des Benutzers tun.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="6cf9a-178">Diesen Elementen ist kein integriertes Verhalten zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="6cf9a-179">Stattdessen müssen Sie die Werte abrufen, die der Benutzer eingegeben oder ausgewählt hat, und etwas mit ihnen tun.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="6cf9a-180">Das erfahren Sie in diesem Tutorial.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="6cf9a-181">**HTML5- und Eingabeformulare**</span><span class="sxs-lookup"><span data-stu-id="6cf9a-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="6cf9a-182">Wie Sie vielleicht wissen, befindet sich HTML im Übergang, und die neueste Version (HTML5) bietet Unterstützung für intuitivere Möglichkeiten für Benutzer, Informationen einzugeben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="6cf9a-183">In HTML5 können Sie beispielsweise (der Seitenentwickler) der Seite mitteilen, dass der Benutzer ein Datum eingeben soll.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="6cf9a-184">Der Browser kann dann automatisch einen Kalender anzeigen, anstatt dass der Benutzer ein Datum manuell eingeben muss.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="6cf9a-185">HTML5 ist jedoch neu und wird noch nicht in allen Browsern unterstützt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="6cf9a-186">ASP.NET WebPages unterstützt HTML5-Eingaben in dem Maße, wie es der Browser des Benutzers tut.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="6cf9a-187">Eine Vorstellung der neuen Attribute `<input>` für das Element in HTML5 finden Sie unter [HTML-Eingabetyp &lt;&gt; Attribut](http://www.w3schools.com/html/html_form_input_types.asp) auf der W3Schools-Website.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="6cf9a-188">Erstellen des Formulars</span><span class="sxs-lookup"><span data-stu-id="6cf9a-188">Creating the Form</span></span>

<span data-ttu-id="6cf9a-189">Öffnen Sie in WebMatrix im Arbeitsbereich **Dateien** die Seite *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="6cf9a-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="6cf9a-190">Fügen Sie `</h1>` nach dem `<div>` schließenden `grid.GetHtml` Tag und vor dem öffnenden Tag des Aufrufs das folgende Markup hinzu:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="6cf9a-191">Dieses Markup erstellt ein Formular mit `searchGenre` einem Textfeld mit dem Namen und einer Schaltfläche zum Absenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="6cf9a-192">Das Textfeld und die Schaltfläche `<form>` "Senden" sind in ein Element eingeschlossen, dessen `method` Attribut auf `get`festgelegt ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="6cf9a-193">(Denken Sie daran, dass, wenn Sie das `<form>` Textfeld und die Schaltfläche "Senden" nicht in einem Element einfügen, nichts gesendet wird, wenn Sie auf die Schaltfläche klicken.) Sie verwenden `GET` das Verb hier, weil Sie ein Formular erstellen, das keine Änderungen auf dem Server vornimmt – es führt nur zu einer Suche.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="6cf9a-194">(Im vorherigen Tutorial haben `post` Sie eine Methode verwendet, d. h., wie Sie Änderungen an den Server übermitteln.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="6cf9a-195">Das sehen Sie im nächsten Tutorial erneut.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="6cf9a-196">Führen Sie die Seite aus.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-196">Run the page.</span></span> <span data-ttu-id="6cf9a-197">Obwohl Sie kein Verhalten für das Formular definiert haben, können Sie sehen, wie es aussieht:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Filmseite mit Suchfeld für Genre](form-basics/_static/image3.png)

<span data-ttu-id="6cf9a-199">Geben Sie einen Wert in das Textfeld ein, z. B. "Komödie".</span><span class="sxs-lookup"><span data-stu-id="6cf9a-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="6cf9a-200">Klicken Sie dann auf **Genre suchen**.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="6cf9a-201">Notieren Sie sich die URL der Seite.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-201">Take note of the URL of the page.</span></span> <span data-ttu-id="6cf9a-202">Da Sie `<form>` das Attribut `method` des `get`Elements auf festlegen, ist der eingegebene Wert nun Teil der Abfragezeichenfolge in der URL, wie folgt:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="6cf9a-203">Lesen von Formularwerten</span><span class="sxs-lookup"><span data-stu-id="6cf9a-203">Reading Form Values</span></span>

<span data-ttu-id="6cf9a-204">Die Seite enthält bereits Code, der Datenbankdaten abruft und die Ergebnisse in einem Raster anzeigt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="6cf9a-205">Nun müssen Sie Code hinzufügen, der den Wert des Textfelds liest, damit Sie eine SQL-Abfrage ausführen können, die den Suchbegriff enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="6cf9a-206">Da Sie die Methode des `get`Formulars auf festlegen, können Sie den Wert lesen, der in das Textfeld eingegeben wurde, indem Sie Code wie den folgenden verwenden:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="6cf9a-207">Das `Request.QueryString` Objekt `QueryString` (die `Request` Eigenschaft des Objekts) enthält die Werte `GET` von Elementen, die als Teil des Vorgangs übermittelt wurden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="6cf9a-208">Die `Request.QueryString` Eigenschaft enthält eine *Auflistung* (eine Liste) der Werte, die im Formular gesendet werden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="6cf9a-209">Um einen einzelnen Wert abzuerhalten, geben Sie den Namen des gewünschten Elements an.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="6cf9a-210">Aus diesem Grund müssen Sie `name` ein `<input>` Attribut`searchTerm`für das Element ( ) haben, das das Textfeld erstellt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="6cf9a-211">(Weitere Informationen `Request` zum Objekt finden Sie später in der [Seitenleiste.)](#BKMK_TheRequestObject)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="6cf9a-212">Es ist einfach genug, den Wert des Textfelds zu lesen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="6cf9a-213">Aber wenn der Benutzer überhaupt nichts in das Textfeld eingegeben hat, aber trotzdem auf **Suchen** geklickt hat, können Sie diesen Klick ignorieren, da nichts zu suchen ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="6cf9a-214">Der folgende Code ist ein Beispiel, das zeigt, wie diese Bedingungen implementiert werden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="6cf9a-215">(Sie müssen diesen Code noch nicht hinzufügen; Sie werden dies gleich tun.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="6cf9a-216">Der Test bricht auf diese Weise zusammen:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="6cf9a-217">Abrufen des `Request.QueryString["searchGenre"]`Werts von , nämlich `<input>` des `searchGenre`Werts, der in das Element mit dem Namen eingegeben wurde.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="6cf9a-218">Finden Sie heraus, ob es `IsEmpty` leer ist, indem Sie die Methode verwenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="6cf9a-219">Diese Methode ist die Standardmethode, um zu bestimmen, ob etwas (z. B. ein Formularelement) einen Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="6cf9a-220">Aber wirklich, Sie kümmern sich nur, wenn es *nicht* leer ist, daher ...</span><span class="sxs-lookup"><span data-stu-id="6cf9a-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="6cf9a-221">Fügen `!` Sie den Operator `IsEmpty` vor dem Test hinzu.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="6cf9a-222">(Der `!` Operator bedeutet logisches NOT).</span><span class="sxs-lookup"><span data-stu-id="6cf9a-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="6cf9a-223">Im Klartext: `if` Die gesamte Bedingung übersetzt sich wie folgt: *Wenn das searchGenre-Element des Formulars nicht leer ist, dann ...*</span><span class="sxs-lookup"><span data-stu-id="6cf9a-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="6cf9a-224">Dieser Block legt die Bühne zum Erstellen einer Abfrage fest, die den Suchbegriff verwendet.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="6cf9a-225">Dies erledigen Sie im nächsten Abschnitt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="6cf9a-226">**Das Anforderungsobjekt**</span><span class="sxs-lookup"><span data-stu-id="6cf9a-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="6cf9a-227">Das `Request` Objekt enthält alle Informationen, die der Browser an Ihre Anwendung sendet, wenn eine Seite angefordert oder übermittelt wird.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="6cf9a-228">Dieses Objekt enthält alle Informationen, die der Benutzer bereitstellt, z. B. Textfeldwerte oder eine Datei zum Hochladen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="6cf9a-229">Es enthält auch alle Arten von zusätzlichen Informationen, wie Cookies, Werte in der URL-Abfragezeichenfolge (falls vorhanden), den Dateipfad der Seite, die ausgeführt wird, den Browsertyp, den der Benutzer verwendet, die Liste der Sprachen, die im Browser festgelegt sind, und vieles mehr.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="6cf9a-230">Das `Request` Objekt ist eine *Auflistung* (Liste) von Werten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="6cf9a-231">Sie erhalten einen individuellen Wert aus der Auflistung, indem Sie ihren Namen angeben:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="6cf9a-232">Das `Request` Objekt macht tatsächlich mehrere Teilmengen verfügbar.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="6cf9a-233">Beispiel:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-233">For example:</span></span>
> 
> - <span data-ttu-id="6cf9a-234">`Request.Form`gibt Ihnen Werte aus `<form>` Elementen innerhalb des `POST` übermittelten Elements an, wenn es sich bei der Anforderung um eine Anforderung handelt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="6cf9a-235">`Request.QueryString`gibt Nur die Werte in der Abfragezeichenfolge der URL an.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="6cf9a-236">(In einer `http://mysite/myapp/page?searchGenre=action&page=2`URL `?searchGenre=action&page=2` wie ist der Abschnitt der URL die Abfragezeichenfolge.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="6cf9a-237">`Request.Cookies`Auf die vom Browser gesendeten Cookies zugreifen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="6cf9a-238">Um einen Wert zu erhalten, von dem Sie `Request["name"]`wissen, dass er sich im gesendeten Formular befindet, können Sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="6cf9a-239">Alternativ können Sie die spezifischeren `Request.Form["name"]` Versionen `POST` (für `Request.QueryString["name"]` Anforderungen) oder (für `GET` Anforderungen) verwenden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="6cf9a-240">Natürlich ist *der Name* der Name des zu erhaltenden Elements.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="6cf9a-241">Der Name des Elements, das Sie erhalten möchten, muss innerhalb der von Ihnen verwendenden Sammlung eindeutig sein.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="6cf9a-242">Aus diesem Grund `Request` stellt das Objekt `Request.Form` `Request.QueryString`die Teilmengen wie und bereit.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="6cf9a-243">Angenommen, Ihre Seite enthält `userName` ein Formularelement mit `userName`dem Namen und *auch* ein Cookie mit dem Namen .</span><span class="sxs-lookup"><span data-stu-id="6cf9a-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="6cf9a-244">Wenn Sie `Request["userName"]`erhalten, ist unklar, ob Sie den Formularwert oder das Cookie verwenden möchten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="6cf9a-245">Wenn Sie jedoch `Request.Form["userName"]` `Request.Cookie["userName"]`oder erhalten, werden Sie explizit zu dem Wert, den Sie erhalten sollen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="6cf9a-246">Es ist eine gute Praxis, spezifisch zu `Request` sein und die Teilmenge `Request.Form` `Request.QueryString`von dem zu verwenden, an dem Sie interessiert sind, wie oder .</span><span class="sxs-lookup"><span data-stu-id="6cf9a-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="6cf9a-247">Für die einfachen Seiten, die Sie in diesem Tutorial erstellen, macht es wahrscheinlich keinen wirklichen Unterschied.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="6cf9a-248">Wenn Sie jedoch komplexere Seiten erstellen, `Request.Form` `Request.QueryString` können Sie die explizite Version verwenden oder Ihnen helfen, Probleme zu vermeiden, die auftreten können, wenn die Seite ein Formular (oder mehrere Formulare), Cookies, Abfragezeichenfolgenwerte usw. enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="6cf9a-249">Erstellen einer Abfrage mithilfe eines Suchbegriffs</span><span class="sxs-lookup"><span data-stu-id="6cf9a-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="6cf9a-250">Nachdem Sie nun wissen, wie Sie den Vom Benutzer eingegebenen Suchbegriff abrufen, können Sie eine Abfrage erstellen, die ihn verwendet.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="6cf9a-251">Denken Sie daran, dass Sie eine SQL-Abfrage verwenden, die wie folgt aussieht, um alle Filmelemente aus der Datenbank zu erhalten:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="6cf9a-252">Um nur bestimmte Filme zu erhalten, müssen `Where` Sie eine Abfrage verwenden, die eine Klausel enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="6cf9a-253">Mit dieser Klausel können Sie eine Bedingung festlegen, für die Zeilen von der Abfrage zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="6cf9a-254">Hier sehen Sie ein Beispiel:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="6cf9a-255">Das Grundformat `WHERE column = value`ist .</span><span class="sxs-lookup"><span data-stu-id="6cf9a-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="6cf9a-256">Sie können neben nur `=`, `>` wie (größer `<` als), `<>` (weniger als), (nicht gleich), `<=` (weniger als oder gleich) usw. verschiedene Operatoren verwenden, je nachdem, was Sie suchen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="6cf9a-257">Falls Sie sich fragen, SQL-Anweisungen &mdash; `SELECT` sind nicht `Select` Groß-/Kleinschreibung ist die gleiche wie (oder sogar `select`).</span><span class="sxs-lookup"><span data-stu-id="6cf9a-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="6cf9a-258">Menschen nutzen Schlüsselwörter jedoch häufig in einer `SELECT` `WHERE`SQL-Anweisung, wie und , um das Lesen zu erleichtern.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="6cf9a-259">Übergeben des Suchbegriffs als Parameter</span><span class="sxs-lookup"><span data-stu-id="6cf9a-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="6cf9a-260">Die Suche nach einem bestimmten`WHERE Genre = 'Action'`Genre ist einfach genug ( ), aber Sie möchten in der Lage sein, nach jedem Genre zu suchen, das der Benutzer eingibt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="6cf9a-261">Dazu erstellen Sie als SQL-Abfrage, die einen Platzhalter für den zu durchsuchenden Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="6cf9a-262">Es sieht wie folgt aus:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="6cf9a-263">Der Platzhalter `@` ist das Zeichen gefolgt von Null.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="6cf9a-264">Wie Sie vielleicht vermuten, kann eine Abfrage mehrere Platzhalter `@0` `@1`enthalten, und sie würden den Namen , , `@2`, usw. haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="6cf9a-265">Um die Abfrage einzurichten und den Wert tatsächlich zu übergeben, verwenden Sie den Code wie den folgenden:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="6cf9a-266">Dieser Code ähnelt dem, was Sie bereits getan haben, um Daten im Raster anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="6cf9a-267">Die einzigen Unterschiede sind:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-267">The only differences are:</span></span>

- <span data-ttu-id="6cf9a-268">Die Abfrage enthält einen`WHERE Genre = @0"`Platzhalter ( ).</span><span class="sxs-lookup"><span data-stu-id="6cf9a-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="6cf9a-269">Die Abfrage wird in`selectCommand`eine Variable ( ) eingefügt. zuvor haben Sie die Abfrage `db.Query` direkt an die Methode übergeben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="6cf9a-270">Wenn Sie `db.Query` die Methode aufrufen, übergeben Sie sowohl die Abfrage als auch den Wert, der für den Platzhalter verwendet werden soll.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="6cf9a-271">(Wenn die Abfrage über mehrere Platzhalter verfügen würde, übergeben Sie sie alle als separate Werte an die Methode.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="6cf9a-272">Wenn Sie alle diese Elemente zusammensetzen, erhalten Sie den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="6cf9a-273">**Wichtig!**</span><span class="sxs-lookup"><span data-stu-id="6cf9a-273">**Important!**</span></span> <span data-ttu-id="6cf9a-274">Die Verwendung von `@0`Platzhaltern (z. B. ) zum Übergeben von Werten an einen SQL-Befehl ist für die Sicherheit *äußerst wichtig.*</span><span class="sxs-lookup"><span data-stu-id="6cf9a-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="6cf9a-275">Die Art und Weise, wie Sie es hier sehen, mit Platzhaltern für variable Daten, ist die einzige Möglichkeit, SQL-Befehle zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="6cf9a-276">Erstellen Sie niemals eine SQL-Anweisung, indem Sie literalen Text und Werte, die Sie vom Benutzer erhalten, zusammenstellen (verstellen).</span><span class="sxs-lookup"><span data-stu-id="6cf9a-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="6cf9a-277">Durch das Verschließen von Benutzereingaben in einer SQL-Anweisung wird Ihre Website zu einem *SQL-Injektionsangriff* geöffnet, bei dem ein böswilliger Benutzer Werte an Ihre Seite sendet, die Ihre Datenbank hacken.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="6cf9a-278">(Weitere Informationen finden Sie im Artikel [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) auf der MSDN-Website.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="6cf9a-279">Aktualisieren der Filmseite mit Suchcode</span><span class="sxs-lookup"><span data-stu-id="6cf9a-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="6cf9a-280">Jetzt können Sie den Code in der Datei *Movies.cshtml* aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="6cf9a-281">Ersetzen Sie zunächst den Code im Codeblock oben auf der Seite durch diesen Code:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="6cf9a-282">Der Unterschied besteht darin, dass Sie `selectCommand` die Abfrage in die `db.Query` Variable eingefügt haben, an die Sie später übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="6cf9a-283">Wenn Sie die SQL-Anweisung in eine Variable eintragen, können Sie die Anweisung ändern, was Sie tun werden, um die Suche durchzuführen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="6cf9a-284">Sie haben auch diese beiden Zeilen entfernt, die Sie später wieder einstecken werden:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="6cf9a-285">Sie möchten die Abfrage noch nicht ausführen (d. h. anrufen `db.Query`) und `WebGrid` sie möchten den Helfer auch noch nicht initialisieren.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="6cf9a-286">Sie werden diese Dinge tun, nachdem Sie herausgefunden haben, welche SQL-Anweisung ausgeführt werden muss.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="6cf9a-287">Nach diesem umgeschriebenen Block können Sie die neue Logik für die Verarbeitung der Suche hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="6cf9a-288">Der ausgefüllte Code sieht wie folgt aus.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-288">The completed code will look like the following.</span></span> <span data-ttu-id="6cf9a-289">Aktualisieren Sie den Code auf Ihrer Seite, damit er mit diesem Beispiel übereinstimmt:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="6cf9a-290">Die Seite funktioniert jetzt so.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-290">The page now works like this.</span></span> <span data-ttu-id="6cf9a-291">Jedes Mal, wenn die Seite ausgeführt `selectCommand` wird, öffnet der Code die Datenbank, `Movies` und die Variable wird auf die SQL-Anweisung festgelegt, die alle Datensätze aus der Tabelle abruft.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="6cf9a-292">Der Code initialisiert `searchTerm` auch die Variable.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="6cf9a-293">Wenn die aktuelle Anforderung jedoch einen `searchGenre` Wert für `selectCommand` das Element enthält, wird der Code `Where` auf eine andere Abfrage festgelegt, d. h. auf eine Abfrage, die die Klausel enthält, nach einem Genre zu suchen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="6cf9a-294">Es setzt `searchTerm` auch auf das, was für das Suchfeld übergeben wurde (was nichts sein könnte).</span><span class="sxs-lookup"><span data-stu-id="6cf9a-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="6cf9a-295">Unabhängig davon, welche `selectCommand`SQL-Anweisung in `db.Query` ist, ruft der Code `searchTerm`dann auf, um die Abfrage auszuführen, und übergibt sie, was auch immer in ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="6cf9a-296">Wenn nichts in `searchTerm`ist, spielt es keine Rolle, denn in diesem Fall gibt `selectCommand` es keinen Parameter, an den der Wert trotzdem übergeben werden kann.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="6cf9a-297">Schließlich initialisiert der Code `WebGrid` den Hilfshelfer mithilfe der Abfrageergebnisse, genau wie zuvor.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="6cf9a-298">Sie können sehen, dass Sie, indem Sie die SQL-Anweisung und den Suchbegriff in Variablen setzen, dem Code Flexibilität hinzugefügt haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="6cf9a-299">Wie Sie später in diesem Tutorial sehen werden, können Sie dieses grundlegende Framework verwenden und weiterhin Logik für verschiedene Arten von Suchvorgängen hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="6cf9a-300">Testen der Search-by-Genre-Funktion</span><span class="sxs-lookup"><span data-stu-id="6cf9a-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="6cf9a-301">Führen Sie in WebMatrix die Seite *Movies.cshtml* aus.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="6cf9a-302">Sie sehen die Seite mit dem Textfeld für Genre.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="6cf9a-303">Geben Sie ein Genre ein, das Sie für einen Ihrer Testdatensätze eingegeben haben, und klicken Sie dann auf **Suchen**.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="6cf9a-304">Dieses Mal sehen Sie eine Liste nur der Filme, die zu diesem Genre passen:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-304">This time you see a listing of just the movies that match that genre:</span></span>

![Film-Seitenliste nach der Suche nach Genre 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="6cf9a-306">Geben Sie ein anderes Genre ein und suchen Sie erneut.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-306">Enter a different genre and search again.</span></span> <span data-ttu-id="6cf9a-307">Versuchen Sie, das Genre einzugeben, indem Sie alle Kleinbuchstaben oder alle Großbuchstaben verwenden, damit Sie sehen können, dass die Suche nicht groß ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="6cf9a-308">"Erinnern" Was der Benutzer eingegeben hat</span><span class="sxs-lookup"><span data-stu-id="6cf9a-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="6cf9a-309">Sie haben vielleicht bemerkt, dass Sie, nachdem Sie ein Genre eingegeben und auf **"Genre suchen"** geklickt haben, eine Auflistung für dieses Genre gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="6cf9a-310">Das Suchtextfeld war &mdash; jedoch leer, mit anderen Worten, es erinnerte sich nicht daran, was Sie eingegeben hatten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="6cf9a-311">Es ist wichtig zu verstehen, warum dieses Verhalten auftritt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="6cf9a-312">Wenn Sie eine Seite senden, sendet der Browser eine Anforderung an den Webserver.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="6cf9a-313">Wenn ASP.NET die Anforderung erhält, erstellt er eine brandneue Instanz der Seite, führt den darin ausgeführten Code aus und rendert die Seite dann erneut im Browser.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="6cf9a-314">Tatsächlich weiß die Seite jedoch nicht, dass Sie nur mit einer früheren Version von sich selbst gearbeitet haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="6cf9a-315">Alles, was sie weiß, ist, dass es eine Anfrage, die einige Formulardaten enthalten hatte.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="6cf9a-316">Jedes Mal, wenn &mdash; Sie eine Seite anfordern, ob zum ersten Mal oder durch Senden, &mdash; erhalten Sie eine neue Seite.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="6cf9a-317">Der Webserver verfügt nicht über speicherspeicher für Ihre letzte Anforderung.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="6cf9a-318">Auch ASP.NET nicht, und auch der Browser nicht.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="6cf9a-319">Die einzige Verbindung zwischen diesen separaten Instanzen der Seite sind alle Daten, die Sie zwischen ihnen übertragen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="6cf9a-320">Wenn Sie z. B. eine Seite senden, kann die neue Seiteninstanz die Formulardaten abrufen, die von der vorherigen Instanz gesendet wurden.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="6cf9a-321">(Eine weitere Möglichkeit, Daten zwischen Seiten zu übergeben, ist die Verwendung von Cookies.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="6cf9a-322">Eine formale Möglichkeit, diese Situation zu beschreiben, ist zu sagen, dass Webseiten *zustandslos*sind.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="6cf9a-323">Webserver und die Seiten selbst sowie die Elemente auf der Seite enthalten keine Informationen über den vorherigen Status einer Seite.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="6cf9a-324">Das Web wurde auf diese Weise entwickelt, weil die Aufrechterhaltung des Status für einzelne Anforderungen schnell die Ressourcen von Webservern ausschöpfen würde, die oft Tausende, vielleicht sogar Hunderttausende von Anfragen pro Sekunde verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="6cf9a-325">Deshalb war das Textfeld leer.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-325">So that's why the text box was empty.</span></span> <span data-ttu-id="6cf9a-326">Nachdem Sie die Seite übermittelt haben, ASP.NET eine neue Instanz der Seite erstellt und durch den Code und das Markup ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="6cf9a-327">Es gab nichts in diesem Code, das ASP.NET anwies, einen Wert in das Textfeld zu setzen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="6cf9a-328">Also ASP.NET nichts getan, und das Textfeld wurde ohne Wert gerendert.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="6cf9a-329">Es gibt tatsächlich eine einfache Möglichkeit, dieses Problem zu umgehen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="6cf9a-330">Das Genre, das Sie *is* in das Textfeld &mdash; eingegeben haben, steht Ihnen im Code zur Verfügung, der sich in `Request.QueryString["searchGenre"]`befindet.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="6cf9a-331">Aktualisieren Sie das Markup für `value` das Textfeld, `searchTerm`sodass das Attribut seinen Wert abruft, z. B. in diesem Beispiel:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="6cf9a-332">Auf dieser Seite hätten Sie `value` das Attribut `searchTerm` auch auf die Variable festlegen können, da diese Variable auch das eingegebene Genre enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="6cf9a-333">Aber das `Request` Objekt zu `value` verwenden, um das Attribut wie hier gezeigt festzulegen, ist die Standardmethode, um diese Aufgabe auszuführen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="6cf9a-334">(Angenommen, Sie möchten &mdash; dies in einigen Situationen sogar tun, sie können die Seite *ohne* Werte in den Feldern rendern.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="6cf9a-335">Alles hängt davon ab, was mit Ihrer App vor sich geht.)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="6cf9a-336">Sie können sich den Wert eines Textfelds, das für Kennwörter verwendet wird, nicht "erinnern".</span><span class="sxs-lookup"><span data-stu-id="6cf9a-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="6cf9a-337">Es wäre eine Sicherheitslücke, wenn Benutzer ein Kennwortfeld mithilfe von Code ausfüllen können.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="6cf9a-338">Führen Sie die Seite erneut aus, geben Sie ein Genre ein, und klicken Sie auf **Genre suchen**.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="6cf9a-339">Diesmal sehen Sie nicht nur die Ergebnisse der Suche, sondern das Textfeld merkt sich, was Sie beim letzten Mal eingegeben haben:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Seite, die anzeigt, dass sich das Textfeld an den vorherigen Eintrag "erinnert" hat](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="6cf9a-341">Suchen nach einem beliebigen Wort im Titel</span><span class="sxs-lookup"><span data-stu-id="6cf9a-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="6cf9a-342">Sie können jetzt nach einem beliebigen Genre suchen, aber Sie können auch nach einem Titel suchen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="6cf9a-343">Es ist schwierig, einen Titel genau richtig zu bekommen, wenn Sie suchen, so dass Sie stattdessen nach einem Wort suchen können, das irgendwo in einem Titel angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="6cf9a-344">Dazu verwenden Sie in SQL `LIKE` den Operator und die Syntax wie folgt:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="6cf9a-345">Dieser Befehl erhält alle Filme, deren Titel "Abenteuer" enthalten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="6cf9a-346">Wenn Sie `LIKE` den Operator verwenden, `%` fügen Sie das Platzhalterzeichen als Teil des Suchbegriffs ein.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="6cf9a-347">Die `LIKE 'adventure%'` Suche bedeutet "beginnend mit 'Abenteuer'".</span><span class="sxs-lookup"><span data-stu-id="6cf9a-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="6cf9a-348">(Technisch bedeutet es "Die Saite 'Abenteuer' gefolgt von allem.") In ähnlicher Weise `LIKE '%adventure'` bedeutet der Suchbegriff "alles, was von der Zeichenfolge 'Abenteuer' gefolgt wird", was eine andere Möglichkeit ist, "mit 'Abenteuer' zu enden".</span><span class="sxs-lookup"><span data-stu-id="6cf9a-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="6cf9a-349">Der Suchbegriff `LIKE '%adventure%'` bedeutet daher "mit 'Abenteuer' überall im Titel."</span><span class="sxs-lookup"><span data-stu-id="6cf9a-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="6cf9a-350">(Technisch gesehen "alles im Titel, gefolgt von 'Abenteuer', gefolgt von allem.")</span><span class="sxs-lookup"><span data-stu-id="6cf9a-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="6cf9a-351">Fügen `<form>` Sie innerhalb des Elements das folgende `</div>` Markup direkt unter dem `</form>` schließenden Tag für die Genresuche hinzu (kurz vor dem schließenden Element):</span><span class="sxs-lookup"><span data-stu-id="6cf9a-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="6cf9a-352">Der Code, der diese Suche verarbeitet, ähnelt dem Code für die `LIKE` Genresuche, mit der Ausnahme, dass Sie die Suche zusammenstellen müssen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="6cf9a-353">Fügen Sie diesen `if` Block im Codeblock oben auf `if` der Seite direkt nach dem Block für die Genresuche hinzu:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="6cf9a-354">Dieser Code verwendet dieselbe Logik, die Sie zuvor `LIKE` gesehen haben,`%`mit der Ausnahme, dass die Suche einen Operator verwendet und der Code " " vor und nach dem Suchbegriff abgibt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="6cf9a-355">Beachten Sie, wie es einfach war, der Seite eine weitere Suche hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="6cf9a-356">Alles, was Sie tun mussten, war:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-356">All you had to do was:</span></span>

- <span data-ttu-id="6cf9a-357">Erstellen `if` Sie einen Block, der getestet wurde, um festzustellen, ob das entsprechende Suchfeld einen Wert hatte.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="6cf9a-358">Legen `selectCommand` Sie die Variable auf eine neue SQL-Anweisung fest.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="6cf9a-359">Legen `searchTerm` Sie die Variable auf den Wert fest, der an die Abfrage übergeben werden soll.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="6cf9a-360">Hier ist der komplette Codeblock, der die neue Logik für eine Titelsuche enthält:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="6cf9a-361">Hier ist eine Zusammenfassung der Funktionsweise dieses Codes:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="6cf9a-362">Die `searchTerm` Variablen `selectCommand` und werden oben initialisiert.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="6cf9a-363">Sie legen diese Variablen auf den entsprechenden Suchbegriff (falls vorhanden) und den entsprechenden SQL-Befehl basierend auf den Aktionen des Benutzers auf der Seite fest.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="6cf9a-364">Die Standardsuche ist der einfache Fall, alle Filme aus der Datenbank zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="6cf9a-365">In den `searchGenre` Tests `searchTitle`für und `searchTerm` wird der Code auf den Wert festgelegt, nach dem Sie suchen möchten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="6cf9a-366">Diese Codeblöcke `selectCommand` werden auch auf einen entsprechenden SQL-Befehl für diese Suche festgelegt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="6cf9a-367">Die `db.Query` Methode wird nur einmal aufgerufen, indem `selectedCommand` der SQL-Befehl verwendet wird und welcher Wert auch immer in `searchTerm`ist.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="6cf9a-368">Wenn es keinen Suchbegriff (kein Genre und kein `searchTerm` Titelwort) gibt, ist der Wert von einer leeren Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="6cf9a-369">Das spielt jedoch keine Rolle, da in diesem Fall die Abfrage keinen Parameter benötigt.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="6cf9a-370">Testen der Titelsuchfunktion</span><span class="sxs-lookup"><span data-stu-id="6cf9a-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="6cf9a-371">Jetzt können Sie Ihre abgeschlossene Suchseite testen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-371">Now you can test your completed search page.</span></span> <span data-ttu-id="6cf9a-372">Führen Sie *Movies.cshtml*aus.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="6cf9a-373">Geben Sie ein Genre ein und klicken Sie auf **Genre suchen**.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="6cf9a-374">Das Raster zeigt Filme dieses Genres, wie zuvor.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="6cf9a-375">Geben Sie ein Titelwort ein, und klicken Sie auf **Titel suchen**.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="6cf9a-376">Das Raster zeigt Filme an, die dieses Wort im Titel haben.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-376">The grid displays movies that have that word in the title.</span></span>

![Filmseitenliste nach der Suche nach 'The' im Titel](form-basics/_static/image6.png)

<span data-ttu-id="6cf9a-378">Lassen Sie beide Textfelder leer, und klicken Sie auf eine der beiden Schaltflächen.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="6cf9a-379">Das Raster zeigt alle Filme an.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="6cf9a-380">Kombinieren der Abfragen</span><span class="sxs-lookup"><span data-stu-id="6cf9a-380">Combining the Queries</span></span>

<span data-ttu-id="6cf9a-381">Möglicherweise stellen Sie fest, dass die Suchvorgänge, die Sie durchführen können, exklusiv sind.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="6cf9a-382">Sie können den Titel und das Genre nicht gleichzeitig durchsuchen, auch wenn beide Suchfelder Werte enthalten.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="6cf9a-383">Sie können z. B. nicht nach allen Actionfilmen suchen, deren Titel "Abenteuer" enthält.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="6cf9a-384">(Da die Seite jetzt codiert ist, erhält die Titelsuche Vorrang, wenn Sie Werte für Genre und Titel eingeben.) Um eine Suche zu erstellen, die die Bedingungen kombiniert, müssen Sie eine SQL-Abfrage mit einer Syntax wie der folgenden erstellen:</span><span class="sxs-lookup"><span data-stu-id="6cf9a-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="6cf9a-385">Und Sie müssten die Abfrage ausführen, indem Sie eine Anweisung wie die folgende verwenden (grob gesprochen):</span><span class="sxs-lookup"><span data-stu-id="6cf9a-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="6cf9a-386">Das Erstellen von Logik, um viele Permutationen von Suchkriterien zu ermöglichen, kann ein wenig involviert sein, wie Sie sehen können.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="6cf9a-387">Deshalb hören wir hier auf.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="6cf9a-388">Coming Up Next</span><span class="sxs-lookup"><span data-stu-id="6cf9a-388">Coming Up Next</span></span>

<span data-ttu-id="6cf9a-389">Im nächsten Tutorial erstellen Sie eine Seite, die ein Formular verwendet, auf der Benutzer Filme zur Datenbank hinzufügen können.</span><span class="sxs-lookup"><span data-stu-id="6cf9a-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="6cf9a-390">Vollständige Sendeliste für Filmseite (aktualisiert mit der Suche)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="6cf9a-391">Weitere Ressourcen</span><span class="sxs-lookup"><span data-stu-id="6cf9a-391">Additional Resources</span></span>

- [<span data-ttu-id="6cf9a-392">Einführung in die ASP.NET-Webprogrammierung mit der Razor-Syntax</span><span class="sxs-lookup"><span data-stu-id="6cf9a-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="6cf9a-393">[SQL WHERE-Klausel](http://www.w3schools.com/sql/sql_where.asp) auf der W3Schools-Website</span><span class="sxs-lookup"><span data-stu-id="6cf9a-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="6cf9a-394">[Artikel "Methodendefinitionen"](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) auf der W3C-Website</span><span class="sxs-lookup"><span data-stu-id="6cf9a-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="6cf9a-395">[Zurück](displaying-data.md)
> [Weiter](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="6cf9a-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
